# Red team — lens: state machine and lifecycle

Target: `docs/plans/2026-09-12-block2-hardening-plan.md` (§3 pins P1–P8, P10, P13, P14; tasks T1a, T1b, T3, T5).
Tree: `/home/user/ALN-Ecosystem` @ `acdcd1e2aeba27296a8dc370a79ff3f0bdb72242`, branch `claude/nice-curie-hescfv`, submodules at pins.
Authorities applied in order: `2026-09-04-phase3-c2c3-resolution-dormancy.md` §8 (+§5/§6) → `CONTEXT.md` §2/§4/§5 → `2026-07-09-phase3-1-installation-profile.md` §1–§3 → the tree.
Every row's evidence was re-read on this tree; no number is taken from the census without verification.

## Findings

| id | severity | plan pin or task | objection | evidence | fix |
|---|---|---|---|---|---|
| SB-1 | BLOCKING | P1 / T1b (`dormantServicesFor(profile)`) | P1 computes dormancy from the PROFILE's endpoint families, but every E2E and rung-1 leg boots a GENERATED profile whose `endpoints` block is derived from the pack's endpoint NEEDS, not from the five families. The ALN manifest declares exactly one endpoint (`display.main`); the toy manifest declares `endpoints: {}`. So the generated ALN simulation profile omits `audio.sinks` and `lighting.instruments`, and the generated toy profile omits everything — under P1 that marks `audio`, `sound`, `music`, `lighting` and `bluetooth` DORMANT on the default ALN leg and all six on the toy leg. Every `lighting:*`/`music:*`/`sound:*`/`audio:*`/`bluetooth:*` gm:command is then refused "not installed tonight", and the lighting/sound/music-only cues land in `disabledCueIds` — which kills the 13-assertion rung-1 audit flow (witness flip, MPD play, pw-play, `attention-before-video`) that CS.1 closed on. T1b's red-first list only tests `dormantServicesFor(aln)` and `dormantServicesFor(toyDormant)`, never the generated profiles the suite actually runs, so nothing catches it. | `backend/scripts/lib/simulationProfile.js:35-39` (endpoints built only from `n.kind === 'endpoint'` needs); `ALN-TokenData/pack-manifest.json` `hardware.endpoints` = `{display.main}` only; `backend/tests/e2e/fixtures/packs/toy-heist/pack-manifest.json` `hardware.endpoints: {}`; `backend/tests/e2e/setup/session-env.js:129,141-155` (no explicit pin ⇒ generated `simulation-profile-<packId>.json`); `.github/workflows/test.yml:286-290` (legs set only `E2E_PACK_PATH`); plan §3 P1, §4 T1b | Make the generator declare ALL five families installed (it is the "everything in software" rung), and add a red-first test `dormantServicesFor(generateSimulationProfile(...))` is empty for BOTH packs. State in T1b that the generator's endpoint block is no longer need-derived. |
| SB-2 | BLOCKING | P1 (endpoint → service map) | P1 converts ENDPOINT absence into SERVICE dormancy via a hardcoded map. That contradicts the ratified vocabulary and the C1 table on two counts. (a) `CONTEXT.md` §5 "Endpoints vs stack": "A missing stack service is a *fault*; an absent endpoint makes its *features* dormant" — features, not the service; C1 §2 row 3 likewise says "dependent cues/surfaces disabled at session start". (b) The C1 table is keyed **pack requirement × profile state**; `dormantServicesFor(profile)` drops the pack side entirely, so `lighting` goes dormant because the profile omits `lighting.instruments` even though the ALN manifest never declares that endpoint as a need. Meanwhile the pack's own authored statement for exactly this — `hardware.stack.<svc>.onAbsent: degrade` — is collected into the needs list and then silently discarded by the resolver. P1 invents a code-resident map where a ratified data declaration already exists and is unread. | `CONTEXT.md` §5 "Endpoints vs stack"; C1 `2026-07-09-phase3-1-installation-profile.md` §2 table row 3; `backend/src/gameRules/packNeeds.js:19-27` (`onAbsent` captured on `kind:'service'`); `backend/src/gameRules/resolution.js:135-150` (`case 'service'` never reads `need.onAbsent`: "one that is not running is a FAULT, never dormant"); `ALN-TokenData/pack-manifest.json` `hardware.stack` (4 entries, all `onAbsent: degrade`) | Either (i) drive service dormancy from `hardware.stack.<svc>.onAbsent` × profile/health, keeping endpoint absence at FEATURE level (cue/command disables), or (ii) argue the service-level shortcut explicitly in the plan as a deliberate refinement of §5's M6 adjudication and reconcile the dead `stack.onAbsent` in the same task. Silence is not an option — the map is the unit's foundation. |
| SB-3 | BLOCKING | P4 + P12 (dormancy set churn mid-show) | A clock cue skipped because it is dormancy-disabled never enters `firedClockCues` — the matcher `continue`s before the fired-set is touched. `service:in-service` (P12) recomputes and removes cues from the dormancy set MID-SHOW, which is the ratified use of the second door ("the TV died and the show goes on without it"). On the next one-second tick, `findMatchingClockCues` matches EVERY past-due clock cue at once and `fireCue`s them in a burst. The engine already has the exact guard for this class (`_markPastClockCuesFired`, used by `restore()` and `loadCues()` under E1's mark-don't-fire rule); P4/P12 neither apply it nor mention the hazard. | `backend/src/services/cue/standingEvaluator.js:187-217` (`if (disabledCues.has(cue.id)) continue;` precedes `firedClockCues`; `elapsedSeconds >= threshold`); `backend/src/services/cueEngineService.js:290` (matcher call), `:869-870` + `:879+` (`_markPastClockCuesFired`), `:855-862` (E1 rationale: "must neither replay past cues (catch-up storm)"); plan P4, P12 | When a cue id LEAVES the dormancy-disabled set while a session is active, mark it fired if its threshold has passed (reuse `_markPastClockCuesFired` scoped to the removed ids). Pin it red-first in T3 alongside `service:in-service`. |
| SB-4 | BLOCKING | P10 (Chromium gets ProcessMonitor) | `hideScoreboard()` deliberately MINIMIZES the kiosk window and leaves Chromium alive for fast re-show; the process being dead while hidden is the normal, harmless state. Putting Chromium under ProcessMonitor makes any crash auto-relaunch it with `--kiosk` — fullscreen, on top — which during a video is a show-breaking cover. Second, ProcessMonitor's `_killOrphan` is strictly weaker than `_doLaunch`'s own recovery (PID-file + `/proc` check + SIGTERM, versus `_doLaunch`'s additional `pkill -9 -f 'chromium.*--kiosk'` and 2 s settle for the single-instance lock), so a supervised restart races the lock, fails instantly, and marches to `gave-up` in five attempts. Third, P10 itself says the display is not a ninth registry service — so `gave-up` has nothing to report `down` to and `service:restart {serviceId}` has no id for it, leaving Chromium with supervision but no red row and no Restart verb, the exact opposite of R-C3-3. | `backend/src/utils/displayDriver.js:258-277` (hide = `windowminimize`, process stays alive), `:147-166` (`--kiosk` spawn), `:127-140` (pkill fallback + 2 s settle), `:168-178` (exit handler only nulls state); `backend/src/utils/processMonitor.js:180-192` (`_killOrphan`), `:141-145` (`gave-up` then stop); spec §8 R-C3-3 | Drop Chromium from P10, or supervise it with a display-aware policy: restart only when the scoreboard is supposed to be VISIBLE, reuse `_doLaunch`'s orphan sweep as the monitor's pre-start hook, and give the display a first-class escalation target and Restart verb (a preflight row alone cannot carry a verb). |
| SB-5 | BLOCKING | P10 (flap window "regardless of output") | Three of the five `ProcessMonitor` consumers are long-running OBSERVERS — `pactl subscribe`, the BlueZ `dbus-monitor`, and the MPRIS `dbus-monitor` — whose entire job is to emit data and which legitimately exit when their bus restarts. Today `receivedData` resets the counter for exactly them, so they retry indefinitely, which is correct. P10 removes that reset for every consumer and caps exits inside `flapWindowMs`; a pipewire reload or a `bluetoothd` restart bursting five exits in 60 s then permanently stops observation, and P10's "`gave-up` → owning service reports `down`" turns a benign bus hiccup into a red `audio`/`bluetooth`/`vlc` row. P11's strategy table names only `vlc`, `mpd`, `chromium`, `haWebSocket`, `haContainer` — the three observers get the new semantics with no strategy entry and fall to the shipped 5/60 s defaults. | `backend/src/utils/processMonitor.js:97-121,136-147` (`receivedData` → counter reset); `backend/src/services/audioRoutingService.js:789-794`, `backend/src/services/bluetoothService.js:497-502`, `backend/src/services/mprisPlayerBase.js:205-211` (the three observer monitors); plan P10, P11 | Give ProcessMonitor a per-instance `supervisionKind` ('service' or 'observer'): observers keep unbounded restart with backoff and never escalate; only the spawned SERVICES (vlc, mpd, chromium) get the flap window. Name all five consumers in P11's table so none falls through to a default. |
| SB-6 | BLOCKING | P7 (typed "start anyway" override) | R-C3-1 ratifies the require gate *with* "a typed, logged 'start anyway' override". P7 builds the backend half — `startGame({startAnyway, reason})` and the payload fields — but no task in the plan builds the GM-facing half. The scanner sends `session:start` with a literal empty payload, and the scanner file lists for T1a (HealthRenderer + its test), T5 (packLoader/session/tokenManager/connectionManager/backstop/HealthRenderer verbs) and T6 (PreflightRenderer) contain nothing that could collect a typed reason. §7 nevertheless declares T1a "the Block 2a candidate … a session cannot start with required equipment missing" — a shippable stop point at which the GM can be hard-refused with no way through. | `ALNScanner/src/admin/SessionManager.js:38-40` (`sendCommand(this.connection, 'session:start', {})`); plan §3 P7, §4 T1a/T5/T6 file lists, §7 T1a row; spec §8 R-C3-1 | Add the override dialog to T1a's scanner slice (it is the same task that ships the refusal), or move the gate itself to the task that ships its UI. Either way the two must land together. |
| SB-7 | BLOCKING | P14 (heal on every `sync:full`) | The A2 pack-loading design pins a session-boundary rule in the loader's own contract: "a pack published mid-session is not picked up until reload, honoring the session-boundary rule (§3)". P14 fires the heal on EVERY `sync:full`, which is emitted on every connect and every reconnect — so a mid-game reconnect after a publish hot-swaps `scoring`, `groups`, `modes`, `entities`, `strings` and `theme` under a session whose earlier transactions were computed against the old rules. R-C2-1 ratified self-healing a *stale* scanner (a connect-time condition); it did not ratify mid-session rule replacement, and `applyPackModes` in particular can change `isConsumingMode()` and therefore duplicate behaviour mid-show. | `ALNScanner/src/core/packLoader.js:21-24` (activation timing doctrine); `ALNScanner/src/core/tokenManager.js:79-103` (the six module-level `applyPack*` mutations); spec §8 R-C2-1; plan P14 | Gate the heal on a session boundary: heal on the FIRST `sync:full` of a connection while no session is active (or while the scanner holds no transactions for the current session), and otherwise surface the mismatch as the backstop screen with "reload between games". Record the refinement against R-C2-1 explicitly. |
| SB-8 | BLOCKING | P14 (`packLoader.refresh()`) | The staged-refresh sequence writes the pointer and then GCs every other `aln-pack-*` cache **before** any consumer validates the new pack. `tokenManager.loadDatabase()` refuses a pack whose `schemaVersion` is not exactly 2 and returns `false` (it does not throw). So a server pack the scanner cannot read — a schemaVersion bump, or any game.json the scanner declines — leaves the pointer flipped to the unreadable hash with the last-known-good cache already deleted. P14's backstop ("blocking screen … reload") then lands the reload on a poisoned pointer: the cache tier is gone, and the station falls to the BUNDLED tier, silently running build-time rules for the rest of the night. | `ALNScanner/src/core/packLoader.js:258-268` (pointer write then `_deleteOtherPackCaches`), `:170-177` (cache tier), `:180-182` (bundled tier); `ALNScanner/src/core/tokenManager.js:60-66` (schema gate returns `false`) | `refresh()` must validate-then-flip: stage, verify, run the consumer re-apply against the staged content, and only then write the pointer and GC. On re-apply failure, discard the staging cache and leave the pointer untouched. |
| SB-9 | BLOCKING | T1a HealthRenderer collapse rule / P12 verbs | T1a sets the collapse rule to "collapse when every service is healthy or dormant". The collapsed layout renders one static summary line and no per-service rows, and `render()` explicitly does nothing while collapsed and unchanged — there is no expand affordance. So the moment a GM uses the ratified second door (`service:out-of-service`) on an otherwise-healthy rig, the dashboard collapses and the "Put back in service" verb T5 is supposed to render on that row becomes unreachable. The second door turns one-way, and a status with no verb is the dashboard `CONTEXT.md` §4 says the GM scanner must not be. | `ALNScanner/src/ui/renderers/HealthRenderer.js:48-61` (collapse computation and the "collapsed + same mode = no update" branch), `:66-78` (collapsed DOM: summary only, `_serviceEls = {}`); plan T1a renderer spec, P12, T5 verb list; `CONTEXT.md` §4 "Status with verbs" | Collapse only when every service is healthy or PROFILE-dormant; an operator-door dormancy keeps the dashboard expanded. Alternatively make the collapsed summary a toggle. Pin it with a renderer test on an operator-door fixture. |
| SM-1 | MAJOR | P5 + P6 (operator latch vs `reset()`) | P5 stores the door on the registry entry and says `reset()` clears latches; P6 says `recompute()` preserves operator-door latches. After `systemReset` those two cannot both hold: `serviceHealthRegistry.reset()` runs at step 4 and the feed at step 6, so by the time `recompute()` runs there is nothing left in the registry to preserve. The plan never says who owns the operator-latch set. As written, `system:reset` — a mid-evening GM action — silently cancels "the TV is out of service", and the next command against that service is refused with a fault message instead. | `backend/src/services/systemReset.js:132` (`serviceHealthRegistry.reset()`), `:236` (cue reload, the feed's stated slot); `backend/src/services/serviceHealthRegistry.js:151-159` (`reset()` routes through `report()`); plan P5, P6 | Give `dormancyService` its own persistent operator-latch set (the authority), with the registry entry as the projection. `recompute()` re-applies from that set after any reset. Add a red-first test: out-of-service survives `system:reset`. |
| SM-2 | MAJOR | P4 (`disabledBy: 'gm' \| 'dormant' \| null`) | The `disabledCues` Set already carries a THIRD meaning the plan never mentions: `fireCue` auto-disables a `once` cue after it fires, from two sites (simple and compound). Under P4 those cues report `disabledBy: 'gm'`, which is false, and the GM's cue list will show a spent once-cue with an "Enable" button that re-arms it as if a human had disabled it. The re-open census's §4 table lists 13 `disabledCues` sites in `cueEngineService.js` and omits both auto-disable calls, so the plan's author had no reason to see it. | `backend/src/services/cueEngineService.js:594-597` and `:694-697` (`if (cue.once) this.disableCue(cueId)`); census `2026-09-12-block2-reopen-census.md` §4 table (no `:596`/`:695` row); plan P4 | Extend the provenance to `'gm' \| 'once' \| 'dormant'` (a third non-persisted-or-persisted decision the plan must make for `once`), or move `once` consumption to its own set. Pin `disabledBy` for a fired once-cue by test. |
| SM-3 | MAJOR | P3 (`cue:fire` acks `success:false` when not fired) | `fireCue` has five distinct non-fire outcomes plus a throw: not-found (throws), disabled (silent return), cycle detected, compound already running, max nesting depth, and **held for a down service**. P3's rule — ack `success:false` with the reason whenever the cue did not fire — collapses the HELD outcome into failure. A held cue is a correct, designed parking that `CONTEXT.md` §4 describes as "the engine parks it and tells the GM"; showing the GM an error toast for it inverts that. M3's ratified intent was specifically "dormancy-disabled fires say 'not installed tonight'", not "every non-fire is a failure". | `backend/src/services/cueEngineService.js:502-553` (the six early returns incl. `holdItem` at `:541-552`); `CONTEXT.md` §4 "Held item"; spec §5 M3 adjudication; plan P3 | Make the return a three-valued outcome `{fired, held?, reason}` and have the ack report held as success with the parked message, refusals as `success:false`. Enumerate every reason string in T1a. |
| SM-4 | MAJOR | P3 return shape / hold policy | `releaseCue()` releases the hold, emits `cue:released`, then `await this.fireCue(...)` and discards the result. Under P3 that fire can now legitimately return `{fired:false}` — the cue was dormancy-disabled by a recompute while it sat held, or a dependency went operator-dormant. The hold is gone, the GM was told "released", and nothing ran. `CONTEXT.md` §4 states the invariant this breaks: "Nothing is silently dropped." P3 lists the quick-fire ack path as the caller to update and misses this one. | `backend/src/services/cueEngineService.js:800-818` (`releaseCue`, return discarded at `:817`); `CONTEXT.md` §4 "Held item"; plan P3, §4 T1a | `releaseCue` must propagate the fire outcome to its `gm:command` ack, and on a refusal either re-hold or report the refusal reason. Add it to T1a's caller sweep. |
| SM-5 | MAJOR | P12 + P13 (holds when a service goes operator-dormant) | P13 says dormant never holds — but only for NEW commands. It is silent about holds that ALREADY exist when the GM takes a service out of service, which is the headline scenario for the second door. Those holds become un-releasable: `releaseCue` refuses with "services still down: vlc" (a fault sentence about a deliberately-dormant service, because `isHealthy` is false for dormant), `videoQueueService.releaseHeld` throws "VLC is still down", and P13's only expiry is at session end. The GM's single escape is Discard, on items the engine told them it was holding for recovery. | `backend/src/services/cueEngineService.js:808-812` (`stillDown` via `registry.isHealthy`); `backend/src/services/videoQueueService.js:914-916`; plan P12 (`service:out-of-service`), P13 | On an operator-door latch, expire every hold blocked by that service immediately (nothing will recover), emitting the discarded events; add it to T3's red-first list beside `service:out-of-service`. |
| SM-6 | MAJOR | T1a/T5 scanner scope (cue surface) | The plan adds `disabledBy` to the wire and a `cue:enable` refusal, but no task touches `CueRenderer.js` — so nothing consumes the new field. A dormancy-disabled standing cue renders identically to a GM-disabled one and gets an "Enable" button the backend will refuse, and the Quick Fire grid ignores disabled state entirely, so a dormancy-disabled quickFire cue stays a live tile whose press now returns an ack failure. The plan's goal sentence is "the engine tells the truth about health"; the cue surface keeps lying about what can run tonight, and DoD (d) only covers the health dashboard. | `ALNScanner/src/ui/renderers/CueRenderer.js:59-86` (quick-fire tiles, no disabled handling), `:104,116-120` and `:144,152-155` (Enable/Disable toggle from `isDisabled`); plan P4, §4 T1a/T5 scanner file lists, §5 DoD (d) | Add CueRenderer to T1a's scanner slice: render dormancy-disabled cues grey with the door's wording, suppress the Enable button (or replace it with "Put the service back in service"), and grey the quick-fire tile. |
| SM-7 | MAJOR | T1a commandExecutor wording | T1a pins ONE dormant rejection sentence — "`<svc>` is not installed tonight (dormant): `<reason>`" — while P5 carries a `door` and T1a's own HealthRenderer spec branches on it ("not installed tonight" / "out of service"). A GM who has just taken the TV out of service will be told it "is not installed tonight". `CONTEXT.md` §4 is explicit that the two doors are the same state with two entrances and different wording; the refusal is the most-read surface of the pair. The same defect is in T1a's summary text ("N/M operational, K not installed"). | `backend/src/services/commandExecutor.js:178-187` (single rejection template); `CONTEXT.md` §4 "Dormant vs fault" (two doors); plan T1a commandExecutor + HealthRenderer specs | One wording helper keyed on `door`, used by the executor, the renderer summary and the row. Pin both doors by test. |
| SM-8 | MAJOR | P6 (`dormantServices` name collision) | The already-built CS.1 rollup returns a `dormantServices` array of endpoint/service ids, under a doc comment that still asserts the SUPERSEDED r1 premise "endpoint ids ARE service ids per the endpoints interior pin" — false since M6+S1 re-keyed endpoints physically. P6 introduces a second `dormantServices`, same name, different element type (`{id, reason, door}`) and different id namespace (registry service ids). T1a's edit to `resolution.js` is scoped to P2 and `installed:false` only, so the stale one survives, and the preflight rows and the require gate will both be reading "a rollup" with two candidate meanings. | `backend/src/gameRules/resolution.js:176-186` (the comment and the filter over `kind === 'service' \|\| 'endpoint'`); spec §5 M6+S1; plan P6, §4 T1a | Rename or retype one of them in T1a and fix the stale comment in the same commit. State which rollup P7's `no-go` test reads. |
| SM-9 | MAJOR | T1a pre-registration vs §7 stop points | T1a puts `service:restart`, `service:out-of-service`, `service:in-service` and `preflight:run` into the AsyncAPI action enum, `REQUIRED_PAYLOAD_FIELDS` and the floor map "so later tasks add switch cases only" — but the switch's `default` answers `Unknown action`. §7 declares T1a a coherent stop point ("the Block 2a candidate"). At that stop the contract advertises four actions the engine rejects as unknown, which is precisely the contract-truth defect class T7's own sweep exists to close. | `backend/src/services/commandExecutor.js:862-867` (`default:` → `Unknown action: ${action}`); plan §4 T1a ("no switch case yet"), §7 T1a row | Either land each action's contract entry with its case (drop the pre-registration and sequence T3/T4 behind T1a's contract file), or have T1a add stub cases returning an explicit "not yet implemented in this build" and mark the enum entries as such. |
| SM-10 | MAJOR | P12 / T1a floor map | Two errors. (a) `preflight:run` matches no prefix in `FLOOR_ACTION_PREFIXES`, so `requiredFloorFunction` returns null and the action is floor-UNPROTECTED — reachable by an observe socket. That is the exact bug the B0 close review fixed once for the `service:` family, and the plan names a floor only for `service:out-of-service`. (b) The three `service:*` verbs need no floor-map work at all: `['service:', 'show-control']` already covers them, so T1a's instruction to register them will produce a redundant entry and may mask (a). | `backend/src/gameRules/grants.js:62-82` (prefix list, `['service:', 'show-control']` at `:78` with its "reachable by an observe socket" comment), `:91-98` (`requiredFloorFunction`); plan T1a, P12 | Name the floor function for `preflight:run` explicitly in P12 (`show-control` is the natural one) and add the prefix; delete the redundant instruction for the `service:*` verbs. |
| SM-11 | MAJOR | P6 (the restore run point) | `sessionService.init()` runs at `app.js:206` — before `bluetoothService/audioRoutingService/lightingService/soundService.init()` (`:211-214`), before MPD and VLC come up, and before `cueEngineService.loadCues(packCues)` (`:246`). A feed there computes its rollup against a registry in which all eight services read `down / "Not yet checked"`, and sets a dormancy cue set on an engine holding zero cues. It is also redundant with the boot run point the same pin places before `startRevalidation` (`:315`), which always runs. The engine already carries the precedent for this ordering hazard (`_restoredClockElapsed` re-applied by `loadCues`). | `backend/src/app.js:206,211-214,246,315`; `backend/src/services/sessionService.js:161-174` (cue-engine restore inside `init`); `backend/src/services/cueEngineService.js:136-140` and `:855-859` (the "sessionService.init() runs before app.js loads cues" deferral); `backend/src/services/serviceHealthRegistry.js:22-29` (all-down seed) | Drop the `sessionService.init` run point. The boot run point at the end of `initializeServices` covers restart-restore (DoD (b)'s "after a restart restore") and sees a fully-initialised registry and a loaded cue set. Keep a feed run only in `createSession`, `systemReset`, boot. |
| SM-12 | MAJOR | P5 + P10 (dormant services are still spawned and supervised) | P5 makes dormancy a presentation state; nothing in the plan stops a dormant service from being initialised, probed or supervised. `lightingService.init()` still starts the HA container and opens the WebSocket, `bluetoothService.init()` still spawns the BlueZ monitor, and VLC still self-spawns. With P10 that means a dormant `vlc` on a rig with no display burns its whole restart budget, hits `gave-up`, and reports `down` with the escalation message — which P5's latch then swallows, so the machine thrashes while the GM sees grey. The interaction is not addressed in either pin. | `backend/src/app.js:211-214,280-298`; `backend/src/services/vlcMprisService.js:111-141` (self-spawn); plan P5 ("`report()` is ignored while latched"), P10 ("on `gave-up` the owning service reports `down`") | Make supervision dormancy-aware: `hostConfigService`/ProcessMonitor consult `registry.isDormant(serviceId)` and do not start or restart a dormant service's child; `service:in-service` starts it. Say so in P10. |
| SM-13 | MAJOR | P11 (host config) vs existing switches | VLC supervision already has an off switch — `VLC_SELF_SPAWN`, whose own code comment says "HOW VLC is supervised is HOST configuration" — and MPD spawn has `ENABLE_MUSIC_PLAYBACK`. P11 adds `host.json supervision.<process>.enabled` for both without reconciling them, and the two do not even mean the same thing (`VLC_SELF_SPAWN=false` means "adopt an externally supervised player", not "don't restart"). Two live sources of truth for one posture is how the fix-vehicle S5 §8.1 incident started. | `backend/src/config/index.js:98` (`vlcSelfSpawn`); `backend/src/services/vlcMprisService.js:102-111,141-143` (the comment and the adopt branch); `backend/src/app.js:226` (`ENABLE_MUSIC_PLAYBACK`); plan P11 | P11 must state the precedence and migration: host.json becomes the single source, the env vars become deprecated aliases read once by `hostConfigService` with a loud warn, and `enabled: false` is defined (adopt-external vs no-restart) per process. |
| SM-14 | MAJOR | P10 (`restart()`) | `start()` is a no-op while `this._proc` is non-null. So P10's `restart()` — specified as "clears the counter and starts" — does nothing when the child is hung but alive, which is exactly the state a GM presses Restart for (a wedged VLC that holds its MPRIS name). The ack would report success. `restart()` also has to clear a pending `_restartTimer`, or the timer fires later and emits a spurious `restarted`, which `vlcMprisService` uses to trigger owner re-resolution. | `backend/src/utils/processMonitor.js:55-57` (`if (this._proc) return;`), `:150-155` (pending timer emits `restarted`); `backend/src/services/vlcMprisService.js:132-139` (`restarted` → `_waitForVlcReady` + `_resolveOwner`); plan P10, P12 | Specify `restart()` as: clear the pending timer, kill the existing child and await its `close`, reset the counter, then `start()` and emit `restarted` so the owner re-resolution hook still runs. |
| SM-15 | MAJOR | §5 DoD (f) / T1b fixtures | DoD (f) requires "a profile that omits a `require` endpoint makes `session:start` refuse", but no pack in the tree declares `onAbsent: require` on any endpoint, and T1b adds only a `degrade` one (`lighting.instruments` on the toy manifest). `resolveOne`'s `case 'endpoint'` returns `no-go` solely on `need.onAbsent === 'require'`, so the require gate is unreachable with the fixtures the plan builds. | `ALN-TokenData/pack-manifest.json` (`display.main` → `onAbsent: degrade`); `backend/tests/e2e/fixtures/packs/toy-heist/pack-manifest.json` (`endpoints: {}`); `backend/src/gameRules/resolution.js:73-92`; plan §4 T1b, §5 DoD (f) | T1b must add a `require` endpoint to the toy manifest (e.g. `display.main: {onAbsent: require}`) and a fixture profile that omits it, or DoD (f) is untestable. |
| SM-16 | MAJOR | P8 + P7 (verdict vocabulary) | The resolver's verdicts are `runs \| dormant \| fault \| no-go` and its rollup is `go \| go-degraded \| no-go`; P8's ROW verdicts are `go \| warn \| no-go \| dormant \| unknown` — there is no `fault`, and the plan never pins the mapping. P7 then throws on "a `no-go` rollup" naming `<problems>`, and the resolver's `problems` list is built from fault AND no-go reasons. Read one way the gate fires only on `require` absences (correct per R-C3-1); read the other way any FAULT — a down `vlc`, an unbound lighting role, a missing sound file — refuses `session:start`, which would block the show on the honestly-absent bluetooth the CS.1 audit records as normal. A build cannot proceed on a coin flip at the unit's headline gate. | `backend/src/gameRules/resolution.js:166-198` (`verdict` values, `rollUp` status and `problems`); plan P7, P8; spec §8 R-C3-1 | Pin the mapping in P8: `fault` → row verdict `warn` (not `no-go`), rollup `no-go` reached only by a `no-go` verdict. State that P7 reads the preflight rollup, not the resolver rollup, and pin it red-first. |
| SM-17 | MAJOR | P1 (bluetooth rule) | "`bluetooth` is dormant unless some declared sink carries `btAddress`" makes the bluetooth service dormant in exactly the situation where the GM most needs it: no known speaker is declared (a swapped or dead speaker), so the pairing panel greys out, every `bluetooth:*` command is refused "not installed tonight", and there is no path to pair a replacement. The service is a CAPABILITY (the adapter and BlueZ), not an endpoint; `bluetoothService.checkHealth()` today reports healthy with an adapter and no devices, which is the honest answer. | plan P1; `backend/src/services/bluetoothService.js:77-105` (`checkHealth` reports healthy with adapter present and no devices; warns when a declared BT sink is missing); `backend/src/services/commandExecutor.js:37-70` (six `bluetooth:*` actions gated on the service) | Drop the btAddress rule. Bluetooth's dormancy, if any, belongs to `audio.sinks` entries with `btAddress` at the SINK/feature level (a declared BT sink that is absent), never to the adapter service. |
| SM-18 | MAJOR | P1 (`audio.sinks → ['audio','sound','music']`) | Mapping one physical family onto three services conflates "no speakers tonight" with "no audio stack". `audio` gates `audio:route:set` and `audio:volume:set`, which include routing and volume for VIDEO audio — a `display.main` concern, a different family. A profile with the TV installed and no separate speakers would therefore play video through HDMI with the volume control greyed out and the `audio` row reading "not installed tonight" while PipeWire is running normally. | plan P1; `backend/src/services/commandExecutor.js:37-70` (`audio:route:set`/`audio:volume:set` → `audio`; `music:*` → `music`; `sound:*` → `sound`); `CONTEXT.md` §5 "Endpoints vs stack" | Split the map: an installed `display.main` implies at least the HDMI sink, so `audio` stays live whenever any sink family member is installed. Reserve `music`/`sound` dormancy for the absence of ALL sinks, and say which. |
| SM-19 | MAJOR | P3 (the dormancy-disable predicate) | "EVERY service-bearing command in it depends on a dormant service" is vacuously TRUE for a cue with ZERO service-bearing commands, so such a cue would be dormancy-disabled by the predicate as written. This is reachable: `display:scoreboard` and `display:return-to-video` are in the cue vocabulary but are not in `SERVICE_DEPENDENCIES`. The mirror problem is that a scoreboard cue can never be dormancy-disabled by ANY profile, because P10 keeps the display out of the registry — so an absent `display.main` silences the video cues and leaves the scoreboard cues pushing to a TV that is not there. | `backend/src/gameRules/cueValidation.js:91-116` (`display:scoreboard`, `display:return-to-video` in `CUE_ACTIONS`); `backend/src/services/commandExecutor.js:37-70` (neither in `SERVICE_DEPENDENCIES`); plan P3, P10 | Restate the predicate as "the cue has at least one service-bearing command AND every one of them depends on a dormant service". Decide separately whether `display:*` cues follow `display.main` — if they do, the display needs a dependency key even without being a registry service. |
| SM-20 | MAJOR | T1a enum sweep (missed consumer) | The enum sweep lists `capabilities.js` but not `assertions.js`, which hard-pins `expect(['healthy','down']).toContain(syncData.serviceHealth.vlc.status)` in the shared `sync:full` structure assertion used across the E2E flows. Any leg on which `vlc` resolves dormant — which under P1 + SB-1 is every generated-profile leg once `display.main` is not declared — fails that assertion with a confusing message. M8's ratified scope was "every `=== 'healthy'` / `'down'` consumer". | `backend/tests/e2e/helpers/assertions.js:183-187`; spec §5 M8 adjudication; plan §4 T1a file list | Add `assertions.js` to T1a's sweep and widen the pin to the three-word enum. |
| SM-21 | MAJOR | P14 (reconnect) | "Reconnects with the new `packHash`" means disconnect-then-connect, and the handshake refuses a GM whose `deviceId` is still listed as connected in the session. The self-heal reconnect therefore races the server's own processing of its disconnect and can be rejected with `DEVICE_ID_COLLISION`, leaving the healed station off the network with a collision error the GM cannot act on. | `backend/src/websocket/socketServer.js:85-107` (collision check over `connectedDevices` with `connectionStatus === 'connected'`); `ALNScanner/src/network/connectionManager.js:207-215,241-248`; plan P14 | Reconnect by re-authenticating on the existing socket path or add a bounded retry that tolerates one `DEVICE_ID_COLLISION`; pin it in T5's heal-path test with a delayed server-side disconnect. |
| SM-22 | MAJOR | P14 (heal loop on a drifted pack) | `/api/pack/manifest` serves the boot-FROZEN manifest while `/api/pack/files/*` reads the pack directory from disk. If a pack file is edited without rebuilding the manifest — the exact drift F-P2-7 exists for, and the mistake CLAUDE.md warns about — every staged refresh fails its per-file sha1 check. The scanner then heals, fails, shows the blocking screen, the GM reloads, the hashes still disagree, and it heals again: a permanent loop with no attempt limit anywhere in P14. | `backend/src/services/packService.js:1254-1269` (frozen `activeManifest` with the disk-drift warn), `:1450-1460` (`resolvePackFile` → disk); `ALNScanner/src/core/packLoader.js:218-224` (sha1 mismatch throws, refresh returns null) | Latch the heal: at most one attempt per `{connection, serverHash}` pair. On the second mismatch for the same server hash, go straight to the backstop screen and stop. Pin it as a T5 test. |
| SM-23 | MAJOR | P8/P7 vs a restored ACTIVE session | A restored active session never passes `startGame`, so the require gate never runs for it — and the documented way to change equipment is a restart, which is exactly this path. The stamp is likewise never refreshed: `session.metadata.preflight` keeps the evaluation from the original start, computed against whatever profile was frozen then. P9 compounds it by defining the `sync:full.preflight` key as "the last evaluation" without saying whether that is the session stamp or the service's last `evaluate()`, so two sources can populate one key. §7's T1a row claims "a session cannot start with required equipment missing" without this caveat. | `backend/src/app.js:206` + `backend/src/services/sessionService.js:104-174` (restore path, no `startGame`); `backend/src/services/sessionService.js:258-...` (the status check is the only gate today); plan P7, P8, P9, §7 | On restore of a non-ended session, re-evaluate and re-stamp with a `restoredAt` marker, and surface a `no-go` as a loud warn plus a GM-visible row rather than a silent stale stamp. Define the `sync:full.preflight` source in P9 as the service's last evaluation, with the session stamp separate. |
| SM-24 | MAJOR | P13 (`processQueue` dormant branch) | Both real entry points to the video queue are gated upstream of `processQueue`: the player-scan route calls `canAcceptVideo()`, which already refuses on `!isHealthy('vlc')` and returns the `409 {status:'rejected'}` wire unchanged for a dormant vlc, and the admin `video:queue:add` is gated by `SERVICE_DEPENDENCIES` → the executor's dormant refusal. P13's "`processQueue` refuses a video when `vlc` is dormant by failing the item" is therefore reachable only for an item already queued when the vlc latch lands. The named T3 test ("dormant vlc video path (no hold, item failed, scan route 409)") would pass without ever entering that branch. | `backend/src/services/videoQueueService.js:866-875` (`canAcceptVideo`), `:97-104` (`processQueue` hold branch); `backend/src/routes/scanRoutes.js:96-102,128-140`; `backend/src/services/commandExecutor.js:37-43,178-187`; plan P13 | Keep the branch (the operator-door-mid-queue case is real) but say so, and write the test to enqueue first and latch dormant second. Note in P13 that the scan-route wire needs no change. |
| Sm-1 | MINOR | P10 cross-reference | P10 says "display liveness is a preflight row (P12)". P12 is "Verbs are commands"; the display-liveness row is specified under T4's preflight arms and the row shape under P8. An implementer following the citation lands in the wrong pin. | plan §3 P10 last sentence vs §3 P12, §4 T4 | Correct the reference to P8/T4. |
| Sm-2 | MINOR | P13 ("emitting the existing discarded events") | `HeldItemsStore.discardAll()` returns nothing and the store emits no events — the `cue:discarded` / `video:discarded` events are emitted by the SERVICES, per item. `expireHolds` cannot reuse "the existing discarded events" from the store as written; it must iterate `getAll()` and call the service-level discard. | `backend/src/services/heldItemsStore.js:204-209` (`discardAll` returns undefined, no emit); `backend/src/services/cueEngineService.js:823-827`, `backend/src/services/videoQueueService.js:924-932` | Specify `expireHolds` as a service-level iteration over `getAll()`/`_heldVideos` calling the existing per-item discard. |
| Sm-3 | MINOR | P13 (`endSession` expiry) | `endSession()` returns immediately when there is no current session, so holds created outside a session (after an end, or during a `system:reset` with no session) never hit that boundary and persist until the NEXT session ends. | `backend/src/services/sessionService.js:455-458` | Put the expiry in a small method both `endSession` and `systemReset` call unconditionally. |
| Sm-4 | MINOR | P5 + systemReset ordering | `serviceHealthRegistry.reset()` at step 4 drives every service through `report(id,'down','Reset')`, each emitting `health:changed` → a `service:state` health push. Until the feed re-latches at step 6 the GM dashboard shows a full red board including the services that are merely dormant — a visible alarm-integrity flicker on every system reset. | `backend/src/services/systemReset.js:132`, `:236`; `backend/src/services/serviceHealthRegistry.js:151-159` | Have `reset()` preserve dormant entries (or re-latch before the step-6 probes) rather than clear-then-rebuild. |
| Sm-5 | MINOR | `service:check` vs dormant | T1a removes "Check Now" from dormant rows, but the `service:check` action itself stays reachable and its ack is built from the probe's boolean — so a dormant service can ack "Health check: lighting = healthy" while its row reads grey "not installed tonight". | `backend/src/services/commandExecutor.js:830-834`; plan T1a renderer spec | Refuse `service:check` on a dormant service with the door wording, or have it ack the dormant state. |
| Sm-6 | MINOR | P15 / T3b | P15 says only "requires an operator or observe token for EVERY connection", but all downstream socket identity — `isAuthenticated`, `tier`, `functions`, `deviceId`, `deviceType`, `version`, `packHash`, the collision check and the packHash mismatch warn — currently lives INSIDE the `deviceType === 'gm'` branch. Hoisting only the token check leaves non-gm sockets authenticated but identity-less. | `backend/src/websocket/socketServer.js:53,109-148` | Say explicitly that the whole identity block moves out of the branch and only the collision check stays GM-scoped. |
| Sm-7 | MINOR | P12 (`service:restart` for vlc) | With `VLC_SELF_SPAWN=false` there is no ProcessMonitor to restart — the engine adopts an external player. P12's vlc verb ("monitor restart + owner re-resolve") has no defined behaviour in that posture. | `backend/src/services/vlcMprisService.js:111,141-143` | Define the verb as owner re-resolve only when self-spawn is off, and say so in the ack. |
| Sm-8 | MINOR | P10 (HA WebSocket bound) | Two issues with bounding the HA reconnect. (a) It is a network reconnect, not a crashed process; R-C3-3's supervisor scope is "crashed software stack services", and bounding it trades away unbounded self-recovery from a transient HA restart for an escalation nobody may be watching. (b) `auth_invalid` sets `_wsStopped` before the close handler runs, so the `down` report in `_handleWsClose` is skipped; the 15 s REST revalidation currently covers it, but under a bounded WS that becomes the terminal path. | `backend/src/services/lightingService.js:212-215` (`auth_invalid`), `:246-264` (`_handleWsClose`, `if (this._wsStopped) return;` before `report`); spec §8 R-C3-3 | Keep the reconnect unbounded with a capped backoff and escalate on ELAPSED time down rather than attempt count; report `down` on the `auth_invalid` path before setting `_wsStopped`. |
| Sm-9 | MINOR | P9 (domain enum site) | The `service:state` `domain` enum is a closed ten-member list. T1a's contract edit list names `DomainStatePreflight` and the `sync:full` keys but never that enum line, and the contract test that validates every domain's producer output will need an eleventh producer. | `backend/contracts/asyncapi.yaml:2342-2348`; `backend/tests/contract/websocket/service-domain-state.test.js`; plan P9, §4 T1a/T4 | Name `asyncapi.yaml:2344` in the contract edit list. |
| SN-1 | NOTE | census input quality | The re-open census's §4 table of `disabledCues` sites in `cueEngineService.js` lists thirteen rows and omits both `once` auto-disable calls (`:596`, `:695`). That omission is the direct cause of SM-2: P4's provenance enum was designed against an incomplete inventory. Worth recording so the census methodology (grep on the variable name, which these two calls do not contain — they call `disableCue()`) is corrected before Block 3's censuses. | `docs/plans/2026-09-12-block2-reopen-census.md` §4; `backend/src/services/cueEngineService.js:594-597,694-697` | Census greps for a seam must cover its mutators by name, not only the field. |

## Checked and found sound (coverage)

- **P1's endpoint interior matches C1 §1 verbatim** — `display.main {installed, output?}`, `audio.sinks [{id, installed, btAddress?, label?}]`, `lighting.instruments {installed, provider}`, `stations {count}`, `personal {expected}` are exactly the ratified shape (`2026-07-09-phase3-1-installation-profile.md` §1, the annotated example). The M6+S1 requirement that the interior be formalised verbatim is met. My objections are to the MAP (SB-2, SM-17, SM-18), not the interior.
- **The plan's three contract line numbers verify exactly** on this tree: `asyncapi.yaml:664` (`sync:full.serviceHealth[*].status`, `[healthy, down]`), `asyncapi.yaml:2597` (`DomainStateHealth`, `[healthy, degraded, down]`), `openapi.yaml:2005` (`GameState`, `[healthy, down]`). The registry's own validator (`serviceHealthRegistry.js:42`) is the fourth site and T1a names it.
- **The two-disable-set split is clean at the persistence seam.** `standingToPersistence(firedClockCues, disabledCues, active)` takes the set as a parameter, so keeping the GM set (and only it) on the persisted call is a one-line discipline, and the plan pins it red-first ("GM set persists, dormancy set does not"). `fromPersistence` returns a fresh Set, so no dormancy id can survive a restore. No leak path found beyond SM-2's `once` conflation.
- **`loadCues()` does not clear `disabledCues`**, so a dormancy set applied before cues load survives the load, and the engine already carries the precedent for deferring restore-time work until cues exist (`_restoredClockElapsed`). A `compute()` reading cues from the frozen pack snapshot rather than the engine is therefore workable — the objection in SM-11 is about the registry snapshot and redundancy, not the cue source.
- **P3's predicate IS computable for compound (timeline) cues.** `fireCue` already walks `cue.timeline ? cue.timeline : cue.commands` and reads `cmd.action` against `SERVICE_DEPENDENCIES`; timeline entries are flat `{at, action, payload}`. Nested cue firing cannot hide a dependency, because `cue:fire` is not in `CUE_ACTIONS` and the cue source refuses off-vocabulary actions. The only defect is the vacuous-truth edge (SM-19).
- **`E2E_PROFILE_PATH` plumbing already exists** (`tests/e2e/setup/session-env.js:129`, `tests/e2e/setup/test-server.js:124,150,224`), so T1b's third Tier L leg is buildable as described; S5's CS.5 debt on this landed.
- **`sync:full.pack` already carries the server's active pack identity** (`syncHelpers.js:146-150`), so P14's premise holds; and `/api/pack/manifest` serves the boot-FROZEN manifest (`packService.js:1254-1269`), so on a clean tree the heal converges after one refresh. The loop in SM-22 needs a drifted pack directory.
- **P13's player-scan 409 needs no code change** for a dormant vlc: `canAcceptVideo()` refuses on `!isHealthy('vlc')` and `scanRoutes.js` already answers `409 {status:'rejected', message:'Video playback unavailable'}`, per decision A5's "final, never queued" wire.
- **A throw from `startGame` surfaces cleanly**: `executeCommand` wraps the whole switch and converts a throw to `{success: false, message}`, which the `gm:command:ack` carries. P7's NO-GO message reaches the GM (the gap is the override path, SB-6).
- **The `cues` / `disabledCues` wire arrays are untyped** (`{type: array}` at `asyncapi.yaml:752-757` and `:2650-2656`), so adding `disabledBy` to a cue summary is not a schema break — only the semantic question of what `getDisabledCues()` returns (union or GM set) is open, and the scanner tolerates either because `CueRenderer` ORs the set membership with `cue.enabled === false`.
- **`isHealthy()` returning false for dormant is the right default** for the consumers I traced: the `SERVICE_DEPENDENCIES` gate, `canAcceptVideo`, `fireCue`'s blocked-service scan and `videoQueueService`'s recoverable-hold listener all want "cannot run right now". The problems are the wording (SM-7) and the hold interactions (SM-5), not the predicate.
- **`ProcessMonitor`'s orphan recovery is PID-reuse-safe** (`/proc/PID/cmdline` verified before SIGTERM) and `stop()` correctly clears the pending restart timer and detaches the `process.on('exit')` handler; P10's additions do not disturb either. The `_processExitHandler` is also correctly re-registered per `start()`, so no listener accumulation across restarts.
- **`['service:', 'show-control']` already floors the three verbs** — verified against `grants.js:78`; only `preflight:run` is uncovered (SM-10).

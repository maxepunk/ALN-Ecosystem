---
name: comms-fixes-plan-execution
description: Execution context + decisions for the 2026-05-28 ALNScanner comms-fixes plan (65-task strict-TDD remediation)
metadata: 
  node_type: memory
  type: project
  originSessionId: 885b6127-942e-4c68-a38e-357e753a06d6
---

Executing `docs/plans/2026-05-28-alnscanner-comms-fixes-plan.md` — 65 tasks (P4d.10 removed), 5 phases, strict RED→GREEN→COMMIT per task. Source-of-truth review doc: `docs/plans/2026-05-28-alnscanner-comms-review.md` (grep finding IDs like RL-1, TQ-2).

**Branches (dedicated implementation branch in BOTH repos):** `fix/alnscanner-comms`
- Parent (ALN-Ecosystem) branched off `docs/alnscanner-comms-fixes-plan` (so the plan doc travels with the implementation).
- ALNScanner submodule branched off `main`.
- Discipline: commit inside ALNScanner submodule FIRST, then `git add ALNScanner` in parent to record the ref bump. Backend contract edits (asyncapi/openapi, Phases 2 + 4a) commit directly in the parent.

**User decisions (2026-06-02):**
- Tracked RED is fine: commit genuinely-failing tests P0.2 (action-enum) + P0.4 (forwarding↔contract) into the main `npm test` suite; they stay red until later phases (4d/contract) turn them green. CI will be red on the feature branch until then — accepted.
- P0.5 (and P1b.2) build-artifact tests go in a SEPARATE `test:build` script (new `jest.build.config.js`, mirror backend's `jest.integration.config.js`); exclude build tests from default `npm test` so the fast unit job stays fast + isn't reddened by the build-test RED. Document the script in ALNScanner/CLAUDE.md + package.json.

**Pre-flight validation (workflow, 2026-06-02):** All Phase 0 anchors verified exact, zero drift. The 3 RED-start tests confirmed genuinely red. Baseline GREEN: 1170 passed / 59 suites. No contradictions; sequencing sound. P0.4 MUST follow P0.3.

**Progress:** Phase 0 + 1 COMPLETE. **PHASE 2 COMPLETE (2026-06-02)** — all P2.1–P2.10 + the 5th amendment, awaiting boundary-review findings + parent ref bump. Cadence: per-sub-phase, multi-lens review at each boundary.

**PHASE 2 execution (Task#32–#41 = P2.1–P2.10):** Backend (parent repo, DIRECT) commits e4e37914 (P2.1 enum), 312395ce (P2.2 — AMENDED: offline emit made contract-complete +tokenId/teamId/points:0, was a real bug), 8f8bcab9 (P2.3 clientTxId echo on result + offline emit), 2b35619d (P2.4 BACKEND HALF = 5th amendment: echo clientTxId on transaction-submit error events — catch reads data.data?.clientTxId, QUEUE_FULL uses scanRequest.clientTxId; added optional clientTxId to asyncapi Error event; WITHOUT this P2.4's scanner test was a PHANTOM MOCK — green test over broken prod fast-fail). Scanner (ALNScanner submodule) 56916d4..0ff70e6: 076c1da (P2.4 scanner: case 'error' + replayTransaction error branch + clientTxId-preferred matching), d17c2a6 (P2.5 lock-in, RED-proven via temp-neutralize+git-checkout), dd516f7 (P2.6 status-branched syncQueue + clientTxId gen + 'kept' log amendment #3), af3d073 (P2.7 always-persist queueTransaction + _submitDurable + _removeByClientTxId; strengthened present-then-absent test = amendment #4), dcd1e8f (P2.8 reconcileWithServerState + syncQueue moved connected→sync:full; fixed mockQueueManager + connected-handler test = blockers #2), be2d9c2 (P2.9 TQ-7 rehydrate/persist/union-in-place + unmark-persist fold-in + per-test localStorage.clear), a0979d5 (P2.10 envelope wrap), 78643fe (coverage tests for _submitDurable branches — ratchet caught 78.9% branches/95.2% functions), 0ff70e6 (service-wiring integration test fix — connected-event-syncQueue regression the pre-flight MISSED because it only scanned networkedSession.test.js, surfaced by FULL-suite run).
- **LESSON (recurring): the full-suite run catches what targeted -t runs miss.** P2.8's connected→sync:full move broke an INTEGRATION sibling (tests/integration/service-wiring.test.js) not in the pre-flight's scanned set. Always run the full ALNScanner suite at a phase boundary, not just the touched files.
- **LESSON: phantom-mock pattern recurred** — P2.4 scanner test mocked an error payload with clientTxId the backend didn't send. Fixed by adding the backend echo FIRST (contract-first 5th amendment). Same class as mpd2/AbortError.
**PHASE 2 BOUNDARY REVIEW (workflow wf_9aadf656-618, 4 lenses: logic/async-race/test-quality/contract).** No catastrophic lost-scan/double-count path; core durability invariant holds; backend dedup is a robust double-count backstop. Found a HIGH I introduced + 3 others. ALL fixed (R1-R6) — submodule 588b4bb, parent 749cc5a6:
- **R1 (HIGH, I introduced it)**: P2.3/P2.4 added clientTxId echo to processed/queued/QUEUE_FULL/catch paths but MISSED 3 sibling emit sites — AUTH_REQUIRED (135), SESSION_NOT_FOUND (206), session-not-active transaction:result (217). Scanner matcher ALWAYS has clientTxId (tokenId+teamId fallback is dead), so unmatched responses HANG the replay 30s — most reachably scanning into a PAUSED session (normal lifecycle). Fixed: echo on all 3 (AUTH reads data.data?.clientTxId, validate hasn't run). Commit cd8c6e5d. LESSON: audit ALL emitWrapped sites for field parity when adding an echoed field.
- **R2 (MEDIUM, cross-session double-count)**: resetForNewSession didn't clear tempQueue → old-session offline scans replay/score into a NEW session (phantom tx). Phase 2 durability widened the window. Fixed: _handleSessionBoundary clears queueManager queue on a genuine NEW session id (same-id restart preserves same-session scans). Commit 588b4bb.
- **R3 (LOW listener leak)**: replayTransaction keyed activeHandlers by tokenId-teamId but matches by clientTxId → concurrent same-token replays collide → leak on timeout. Fixed: key by clientTxId.
- **R5/R6 (test gaps, NO phantom mocks found this round)**: QUEUE_FULL echo test, e2e durable-flow test (real matcher, no replayTransaction mock — phantom-mock-class guard), persist-BEFORE-emit ordering via invocationCallOrder, contract-doc clientTxId-presence test, reconcile empty/no-match coverage.
- Verification: ALNScanner full `npm test` = 1230 passed / 2 tracked-RED / 60 suites; coverage ALL 50 files pass. Backend contract = 198 passed; adminEvents unit = 18. Submodule 588b4bb (clean). **Parent HEAD 749cc5a6 — ALNScanner ref bumped 56916d4→588b4bb. PHASE 2 FULLY COMPLETE.**

**⚠️ PHASE 3 CARRY-OVER (user-approved deferral, MUST include in Phase 3):** Review HIGH — `transaction:failed`/`backend:error` events have NO consumer, and the dedup guard (markTokenAsScanned, app.js:758, set BEFORE submit) is NEVER cleared on permanent failure (unmarkTokenAsScanned has zero networked callers). So a rejected/invalid/paused scan drops the queue entry AND locks the token out of re-scan for the session with no GM feedback = lost-scan-equivalent. PRE-EXISTING (Phase 2 added the events, didn't worsen it) → user approved deferring to Phase 3. **Phase 3 MUST wire a transaction:failed/backend:error listener that (a) SURFACES the error (toast) AND (b) calls dataManager.unmarkTokenAsScanned(tokenId) on NON-duplicate permanent failure (NOT on 'duplicate' — genuinely claimed). The unmark→persist path already exists (P2.9). Verify Phase 3's plan covers the UNMARK, not just the toast — if not, ADD it.**

**LEDGER additions (deferred, low/no live-game impact) → docs/plans/2026-06-02-alnscanner-comms-deferred-followups.md:** (1) resetForNewSession doesn't clear old-session persisted networkedScannedTokens:<oldSid> key (harmless, different key). (2) session-not-active transaction:result emits transactionId:null but contract requires non-null string (pre-existing, untested, not validated by scanner). (3) `rejected` status in enum is dead on the GM WS path (no-session=error event, not-active=transaction:result status 'error'); scanner handles it defensively — decide keep-vs-remove in a later cleanup. (4) No fallback if sync:full never arrives after connect (backend auto-sends it on every connect, so LOW). (5) sync:full re-entrancy: a 2nd sync:full mid-syncQueue can restore reconcile-dropped entries (self-healing via backend dedup → redundant replay, no double-count).

**PHASE 3 COMPLETE (2026-06-02), pending boundary-review findings + parent ref bump.** P3.1-P3.4 (Task#48-#51). Pre-flight workflow wf_0acb3fa9 re-anchored P3.1-3 (all exact, ~±20-line drift only) + designed P3.4. Scanner submodule 588b4bb..ccee7ed; backend (parent) 954c2ddc.
- P3.1 (AC-2, 794afa8): safeAdminAction → uiManager.showError on rejected admin-command promise.
- P3.2 (AC-3, aa5d531): system:reset ack re-arm filter. NOTE: used arrow + self-referencing const (NOT plan's .bind(this) + named-fn-expr, which was BUGGY — re-arm with the bare name re-registers the UNBOUND fn → crash on this.debug.log).
- P3.3 (AUTH-7, 694e419): broadened case 'error' to startsWith('AUTH_')+PERMISSION_DENIED → auth:required + removeItem('aln_auth_token'). Used ADDITIVE form (backend:error stays UNCONDITIONAL) per pre-flight — plan's else-only block was a silent behavior change + had an operator-precedence bug (parenthesized).
- **P3.4 (carry-over, OPTION B — user chose B over A/C). MAJOR CORRECTED-PREMISE FINDING:** the carry-over premise ("permanent failure drops queue entry → unmark on transaction:failed") was WRONG — verified the backend NEVER emits status 'rejected' on the GM WS path; invalid-token AND paused both came as status 'error' → replayTransaction REJECTS → .catch → KEPT+retried (not dropped), transaction:failed NEVER fired (dead). Real gaps: no operator feedback + invalid-token retried-forever + token-locked. Option B fix: BACKEND (954c2ddc) invalid-token → status 'rejected' (transactionService.processScan overrides response.status; transaction model unchanged so isRejected()/'error' semantics intact), paused/not-active STAY 'error' (transient). SCANNER (ccee7ed): networkedSession forwards queueManager transaction:failed → session; app._wireNetworkedSessionEvents consumes → uiManager.showError + dataManager.unmarkTokenAsScanned(tokenId), NOT on 'duplicate'. Blast radius CONTAINED to error-propagation.test.js (5 invalid-token 'error'→'rejected' assertions; paused/not-active assertions in admin-interventions/fr-transaction-processing/cue-engine correctly UNCHANGED). 'rejected' contract description clarified. error-propagation integration test re-run GREEN (10 passed).
- **PHASE 3 BOUNDARY REVIEW (wf_5648bbbd) + fixes COMPLETE.** Core P3.4 chain verified clean (no lost-scan/double-count/wrong-unmark; backend emits ONLY transaction:result for invalid tokens, no separate error event → no double-toast; listener lifecycle correct across transient reconnect). Found 1 MEDIUM + LOWs; the user's pause-semantics decision (GATE at scan time + REJECT in-flight, NOT defer/auto-apply) reshaped the fixes. Review-fix commits (submodule ccee7ed..d4c8a2c): PR3-A gate (a25b54d — processNFCRead blocks networked scan when sessionState.status!=='active'; standalone unaffected); PR3-C (1f64a70 — replayTransaction RESOLVES on transaction:result status 'error' so the in-flight paused scan is DEFINITIVE: remove+unmark+toast, not kept; reverses the planned 'drop dead arm'; only 30s-timeout + type==='error' EVENTS still reject=transient); PR3-B (8ee485d — AC-3 ack re-arm leaked the armed once on the 5s timeout → socket.off in timeout); PR3-E (d4c8a2c — destroy() forwarder-cleanup test). PR3-D (processScan no-session 'rejected'→'error') DROPPED (moot: gate handles no-active-session; 'error'/'rejected' both definitive now; unreachable). Double-replay/double-toast dedup (_inFlight guard) → ledger item O (LOW, narrow reconnect-timing, optional).
- **LESSON: always nail down product/game semantics before "fixing" a kept-vs-rejected ambiguity.** The boundary review's MEDIUM (paused scans kept-but-never-flushed) had TWO opposite valid fixes (defer-and-flush-on-resume vs gate-and-reject); I asked the user (pause = deliberate stop → gate+reject). My initial PR3-A (flush-on-resume) was the WRONG direction — caught by asking.
- Verification: ALNScanner full `npm test` = 1243 passed / 2 tracked-RED (P0.2 P4d.11, P0.4 Phase4a) / 62 suites; coverage ALL 50 files. Backend full unit+contract = 1727 passed / 88 suites (P3.4a only backend change; PR3-D dropped). **Parent HEAD 12953464 (ALNScanner ref bumped 588b4bb→d4c8a2c) + ffdc6002 (ledger). PHASE 3 FULLY COMPLETE.**

NEXT after Phase 3: **Phase 4 — Contract Drift & Remaining Hardening** (plan line 3643+, Phase 4a-4d). **Phase 4a MUST add the P0.4 reconciliation task** (the documented plan GAP: add BatchAck+PlayerScan to asyncapi server→client subscribe oneOf + decide scoreboard:page direction — needs a user decision) to flip P0.4 green. P0.2 flips green at P4d.11 (deletes dead restartSystem/clearData). Re-read each Phase 4 task + re-anchor before executing.
- Phase 1c: submodule 9d71d93..56916d4. P1c.1 auth-POST AbortSignal.timeout(5000)+timeout msg (HTTP-1); P1c.2 _normalizeUrl mirrors page protocol (HTTP-5; test file runs jsdom https via @jest-environment-options since jsdom location is non-configurable); P1c.3 block station assignment when /api/state unreachable, removed lastStationNum counter (RL-7). Review found R10 (56916d4): catch must match TimeoutError (spec browsers) not just AbortError — AbortSignal.timeout() throws TimeoutError; test had hand-injected AbortError (phantom mock). Deferred item I (stale lastStationNum docs) → ledger.
- **Deferred-item functional triage (user pushed: judge by live-game FUNCTIONALITY, not task-ownership).** Surfaced a real miss: R7 made the SW cache live, and tokenManager fetches a same-origin non-hashed tokens.json → cache-first + static cache name would serve a STALE token DB across deploys. FIXED: R9 (f755c6f) SW → network-first (fresh online, cache as offline fallback; subsumes deferred E). Also fixed B (a97c5b9, stop NFC+clear _scanningActive on finishTeam — lost-scan-adjacent) + A (77131a6, recursive conformance scan). Genuinely-deferred (zero live-game impact, none owned by a later task): C/D/F/G/H — tracked in `docs/plans/2026-06-02-alnscanner-comms-deferred-followups.md`. LESSON: "no later task owns it" ≠ "safe to defer" — judge by what breaks in a live game.
- Phase 1b + pulled-forward P4b.1/P4b.2: submodule ac49624..070f0f5. P1b.1 SW-reg→Debug.log; P1b.2 ship runtime-cache sw.js (Vite emit plugin); P1b.3 page-lifecycle controller (RL-2); P4b.1 NFC AbortController; P1b.4 NFC pause/resume on lifecycle. 3-lens review found: HIGH (closeSocket must call cm.disconnect not client.disconnect, else pending reconnect timer fires backgrounded → defeats BFCache — R6/cd36d4e); MEDIUM sw.js isBypass keyed off host/port → matched every prod request → cache inert (R7/070f0f5); MEDIUM P1b.4 re-arm needs NFC-2 idempotency (P4b.2/0e49a39 pulled forward — plan's seq note only pulled P4b.1). Build tests live in tests/build-artifacts/.

**Tracked-RED schedule + a PLAN GAP (flagged 2026-06-02):** P0.5 already green (P1b.2). P0.2 → flips green at **P4d.11** (deletes dead restartSystem/clearData). **P0.4 → NO owning task exists in the plan** — grep of all Phase 4 found zero refs to scoreboard:page/PlayerScan/BatchAck/CC-7; the P0.4 body said "owned by CC-7/CC-3" but CC-7 was never written as a task and CC-3 (P4a.5) only touches the PUBLISH oneOf. ACTION: when we reach **Phase 4a**, ADD a contract-first task to reconcile the SUBSCRIBE oneOf — add BatchAck+PlayerScan to asyncapi.yaml server→client oneOf, and DECIDE scoreboard:page direction (client consumes it → add to MESSAGE_TYPES; or server-internal → remove ScoreboardPage from oneOf) — else P0.4 stays red forever.
- Phase 0: submodule b09c795..29987fe (P0.1–P0.5 + js-yaml devDep). Code review: sound.
- Phase 1a: submodule d0b93a6..b589798 (P1a.1–P1a.6, RL-1/RL-3/AUTH-1/RL-5/RL-6). 3-lens review found a BLOCKER + 2 mediums, fixed in a2eb8c3..75cbdee: (R1 blocker) disconnect() 1s fallback timer wasn't cleared → after P1a.5 await-teardown it killed the freshly-reconnected socket ~1s later (defeated the phase); (R2/M1) error handler now registered BEFORE connect handshake so first-connect AUTH_* re-prompts; (R3/M2) reconnect timer tracked (cancellable+non-stacking); (R4) test-quality nits; (R5, 5e9bade) connect() is now a single-flight wrapper around _doConnect() (dedups concurrent attempts, prevents _lastErrorReason corruption — chose single-flight promise over a bare boolean that would resolve undefined-as-success). Parent HEAD 78eaf405. LESSON: plan's RED/GREEN predictions missed sibling tests AND the await-teardown timer race — multi-lens review earned its keep.

**Build-test dir deviation:** build-artifact tests live in `tests/build-artifacts/` (NOT the plan's `tests/unit/build/` for P0.5, NOR `tests/build/` for P1b.2) — a dir named `build` is gitignored by `.gitignore` `build/`. Put P1b.2's SW test in `tests/build-artifacts/` too. Gated out of `npm test` via `testPathIgnorePatterns`; run via `npm run test:build` (jest.build.config.js). js-yaml is now a direct devDependency (^3.14.2).

**Key facts:** `ALNScanner/dist/` is gitignored (Vite `emptyOutDir:true` wipes it on build — harmless to git). `js-yaml@3.14.1` resolvable from ALNScanner. Cross-phase prereq: do P4b.1 (NFC AbortController) BEFORE P1b.4 (NFC-3). Contract-first: asyncapi/openapi edits land as their own commits before scanner consumers. See [[mpd2-sendcommands-string]] for adjacent music work context.

**Phase 2 pre-flight (workflow wf_c79d6270-86f, 6 clusters, 2026-06-02). Phase 2 tasks = Task#32–#41 (P2.1–P2.10).** Anchors overwhelmingly EXACT; minor prose-only drift (no TDD step breaks): syncQueue spans networkedQueueManager.js:73-138 not 84-122 (unconditional `this.tempQueue=[]` at line 121; `batch` is a spread-copy not `.slice()`); app.js isTokenScanned call at 685 (plan 669) + markTokenAsScanned at 758 (plan 742) — both +16 lines, in processNFCRead/recordTransaction, semantics correct; test describe('queueTransaction') closes at test:145, describe('replayTransaction') = 282-444 (insert P2.4 test before 444); validators gmTransactionSchema = 132-140, validate() = 177-186 (stripUnknown at 180, NO .unknown(false) — only sessionUpdateSchema has it). All RED-start predictions GENUINE except two P2.6 lock-ins (below). deviceScannedTokens CONFIRMED Array<string> of tokenIds (reconcile type-correct). _emitCommand envelope at NetworkedStorage.js:49-55 (P2.10 mirrors). P2.9 cross-file wiring CONFIRMED: initializeNetworkedMode 103(strategy.initialize)/107(_syncScannedTokens); _syncScannedTokens 141-145 shares Set BY REF (`this.scannedTokens = this._activeStrategy.scannedTokens`); resetForNewSession .clear()s in place at 508(UDM)+514(strategy).

**FOUR plan-amendments REQUIRED (pre-flight caught what unit tests wouldn't):**
1. **P2.2 BLOCKER** — offline queued emit (adminEvents.js:175-180) = `{status,queued,transactionId,message}`, MISSING contract-required tokenId/teamId/points (TransactionResult.data.required=[status,transactionId,tokenId,teamId,points,message]). ajv runs allErrors:true+strict:false → `validateWebSocketEvent` FAILS on 3 missing-required errors even AFTER P2.1 widens the enum. Plan's "Step 3 — no production change" is WRONG; P2.2 stays RED. FIX (functional, not just test-green): add `tokenId: scanRequest.tokenId, teamId: scanRequest.teamId, points: 0` to the offline emit (add clientTxId echo there too when P2.3 touches the emit). REAL bug: scanner's tokenId+teamId fallback matcher (P2.6) can't correlate a queued result missing those → 30s hang per backend-offline submit. No existing test locks the incomplete shape (cluster B confirmed) → safe to fix. Chosen approach: fix the emit (NOT loosen the contract).
2. **P2.8 BLOCKER (2 sibling breaks the plan ignored)** — (a) mockQueueManager (networkedSession.test.js:44-47) lacks reconcileWithServerState; existing sync:full test (726-738, passes ['token1','token2']) hits the new reconcile call → TypeError. ADD `reconcileWithServerState: jest.fn()` to the mock. (b) test 297-310 'should initialize admin and sync queue on connected event' asserts syncQueue called on the connected handler; P2.8 removes that call → MOVE/rewrite the assertion onto the sync:full path.
3. **P2.6 BLOCKER** — test 'should clear queue even if some transactions fail' (219-223) asserts `syncCompleteCall[1]` toEqual `{total:2,success:1,failed:1}` EXACT; rewritten syncQueue adds a `kept` key to the log → breaks even after the length fix. Update to include `kept` (or objectContaining). Also 'should replay all transactions' (178-195) needs `{status:'success'}`→`'accepted'` hand-edit (named in plan but no rewrite block; its 193 toHaveLength(0)+194 removeItem stay valid after the switch).
4. **P2.7 WEAK-RED** — 'should remove the entry after a definitive accepted result' passes ACCIDENTALLY pre-fix (connected path never queues → tempQueue.some===id already false). STRENGTHEN: assert entry PRESENT post-queue, then ABSENT post-result. Also the 2× `await Promise.resolve()` microtask flush may be too few ticks for `_submitDurable.then()→_removeByClientTxId` — use a deterministic wait.

**In-flight judgment calls (fold in, no separate task):** P2.6 two lock-ins ('should remove rejected entries', 'should remove accepted and duplicate entries') are GREEN pre-fix not RED (current code clears all unconditionally) → keep as regression guards, expect green at the RED step (plan's -t filter framing is misleading). P2.9: unmarkTokenAsScanned (udm:456-458) does NOT persist → admin-delete-then-reload re-blocks the re-scan (FIX: add persistScannedTokens to unmark — functional, dedup-correctness); new P2.9 test describes MUST clear localStorage per-test (jsdom localStorage persists per-file → cross-test bleed) via fresh Object.defineProperty store like networkedQueueManager.test.js:16-33; resetForNewSession leaks old-session localStorage key (harmless, different key → ledger). P2.1 narrative imprecise (WS no-session path emits 'error' not 'rejected'; processScan 'rejected' largely unreachable via WS GM-submit) — enum still covers 'rejected' for contract completeness, no action. P2.10 naming: plan says "keep _saveScannedTokens()" but no such method exists (P2.9 introduces persistScannedTokens) and addTransaction has no save call today — just wrap the envelope, keep the existing scannedTokens.add, don't reference _saveScannedTokens.

**Phase 4 pre-flight (workflow wf_5b9fe3de-197, 14 agents, 2026-06-02). Phase 4 = P4a(10)+P4b(5)+P4c(8)+P4d(11,.10 removed) + the P0.4 reconciliation gap-task.** Most anchors drifted by line but logic intact. THREE HARD BLOCKERS the plan's own RED/GREEN predictions got wrong:
1. **P4a.3/P4a.4 uuid-fixture BLOCKER** — P4a.4's makeServices() factory uses `session.toJSON()` id:'sess-1', but SyncFull.data.session.id has `format:uuid` (asyncapi.yaml:301-303) and contract-validator loads ajv-formats (enforced even under strict:false). VERIFIED: TEST1 (`.not.toThrow()`) FAILS today AND after the P4a.3 contract edit (3 errors on /data/session: uuid/null/oneOf) — contradicts plan's "may already pass (permissive)". TEST2's RED is mis-described (throws on uuid, not /required/). FIX (mechanical): set factory id to a valid UUID e.g. '2a2f9d45-5d2d-441d-b32c-52c939f3c103' → verified TEST1 passes. The contract edit itself is correct; the 7 added fields match buildSyncFullPayload's real keys.
2. **P4d.9 wrong-test-home BLOCKER** — cited file `backend/tests/contract/scanner/request-schema-validation.test.js` is HTTP-only (imports validateHTTPRequest/getHTTPRequestSchema; NO ajv/gmCommandSchema/asyncapiPath/fs). Plan's proposed doc-assertion+AJV test has no harness there. FIX: relocate the prose-assertion to a test that already loads the YAML (orchestratorClient.test.js does `yaml.load` ~299-300, or a backend asyncapi-contract test) — OR add fs+asyncapiPath scaffolding. The contract-prose change (asyncapi.yaml:1437 `sink:'hdmi'|'bluetooth'`) is valid.
3. **P4d.11 stale-deletion-target (drift, flagged)** — AdminOperations.test.js NO LONGER has `describe('restartSystem')`/`describe('clearData')` at 21-46; live equivalent is `describe('non-contract emergency methods (AC-4)')` at 31-48 whose two tests CALL ops.restartSystem()/clearData() → MUST remove THAT block (following literal plan names leaves it calling undefined methods → RED). Production change (delete the 2 methods, AdminOperations.js:24-38, zero callers) is correct. adminModule.test.js cases at 250-282.

**P4b is mostly DONE:** P4b.1 (AbortController) AND P4b.2 (idempotent stopScan-before-rearm guard, nfcHandler.js:40-42) BOTH already-done (pulled into Phase 1). Only P4b.3 (null/non-string id guard, app.js processNFCRead:669 — only source==='error' guarded, hits result.id.length at 678), P4b.4 (drop url-record branch nfcHandler.js:129-138 + rewrite codifying test :72-85), P4b.5 (log event.type not raw Event @nfcHandler:76; nfcReadErrorCount escalation @app.js:625-630, reset before findToken ~707) remain. P4b.5 soft-depends on P4b.3.

**P0.4 RECONCILIATION RESOLVED (UX intent 100% clear from code → no user UX question needed):** scoreboard:page is DISPLAY-ONLY — consumed solely by backend/public/scoreboard.html:1654 (connects deviceType:'gm', so it's in the gm room); emitted broadcasts.js:444. ZERO inbound listeners in ALNScanner/src (ScoreboardController.js:24/29/38 only SENDS scoreboard:page:next/prev/owner gm:commands — distinct outbound action strings; docstring: "pure passthrough, scoreboards perform the transition client-side"). So its absence from MESSAGE_TYPES is CORRECT. The AsyncAPI `/` subscribe oneOf is the UNION of server→client events across all gm-room clients (GM scanner + scoreboard display), not the GM-scanner subset — so the plan's strict toEqual was a FALSE premise. RECONCILIATION = (a) add BatchAck (msg already at asyncapi.yaml:1722) + PlayerScan (1873) refs to the subscribe oneOf — anchor on literal text `- $ref: '#/components/messages/DisplayMode'` then ScoreboardPage (NOT line nums; P2.1/P4a.3/.5/.7 all edit asyncapi.yaml); (b) reshape P0.4 test (orchestratorClient.test.js ~298-310) from strict toEqual to SUBSET+ALLOWLIST: NON_GM_SUBSCRIBE_EVENTS=['scoreboard:page'] (cite broadcasts.js:444 + scoreboard.html:1654); assert (i) every MESSAGE_TYPES ∈ subscribe set, (ii) every subscribe event ∈ MESSAGE_TYPES OR allowlist [catches WS-2 dropped-event class], (iii) no allowlisted event also in MESSAGE_TYPES. NOTE: batch:ack is in MESSAGE_TYPES (forwarded) but NOT handled in networkedSession.js (no case) — genuine server→client event the GM device room receives (gmAuth.js:86, emitted scanRoutes.js:362); a handler is a SEPARATE concern, out of scope, contract reconcile stands regardless.

**Other Phase 4 drift/amendments (fold in during execution):** P4a.5 — GmCommand msg is large (asyncapi.yaml:1407+), find its true END before inserting SyncRequest. P4a.8 — NEAR-DUPLICATE: existing connectionWizard.test.js:179-198 'should filter out non-GM devices and disconnected GMs' ALREADY covers the CC-8b regression (GM_Station_1 connected + _5 disconnected → _2); production filter live since 2026-03-27 (eac0ebf8); make P4a.8 genuinely additive or skip with rationale. P4a.10 — Step-3b per-line attribution scrambled but "convert ALL 5 object-shape activeScene values" is correct; renderer spots drifted (:104→:96, :113→:104). P4c.3 — a SEPARATE AUTH_INVALID branch (connectionManager.js:162-171) already removeItems, but it's NOT one of the 3 targets (invalid_token:113, max_retries:180, token_expired:235). P4c.5 — shared fixture lacks BOTH #scanServersBtn AND #connectionModal → ADD both or concurrency test throws TypeError in RED+GREEN. P4c.7 — existing happy-path mocks lack `headers` → add `headers:{get:()=>'application/json'}`. P4d.3 — sync:full→StateStore test block moved to 923-1065; mockStore lacks `replace`. P4d.4 — structuredClone available (Node 22). P4d.6 — escapeCssAttrValue.js to-create; jsdom 27.1.0 `window.CSS===undefined` caveat VALID (do NOT use CSS.escape). Re-anchor ALL line refs before editing (most drifted ~15-60 lines).

**P0.4 DECISION (user, 2026-06-02) — OVERRODE my subset+allowlist recommendation.** User chose "forward scoreboard:page too" + "remote-control + de-dup only" + "split (track the big stuff separately)". My code-only read said scoreboard:page was a dead-forward; user's forward-looking INTENT differed — exactly the UX-intent gap [[feedback-clarify-ux-intent]] guards. FINAL P0.4-cluster scope for Phase 4a (slot as new tasks, e.g. P4a.11/.12/.13, contract-first): (1) asyncapi subscribe oneOf += BatchAck(msg@1722)+PlayerScan(@1873), anchor on literal `- $ref: '#/components/messages/DisplayMode'`; (2) MESSAGE_TYPES += 'scoreboard:page' → both sets = 22 identical → **original strict toEqual P0.4 test PASSES as-is, NO subset/allowlist reshape**; (3) minimal scoreboard:page consumer (networkedSession `case` → EvidencePicker confirmation hint; parity-free: reflect owner for action 'owner', brief next/prev confirm otherwise); (4) owner-sort PARITY FIX — GM picker sorts ALPHABETICAL (uDM:301) but display sorts recency-DESC (scoreboard.html:994) → align GM picker to display order; (5) extract GM-internal currency formatter (~8 `$n.toLocaleString()` in uiManager). De-dup is GM-INTERNAL + behavioral-parity ONLY — cross-component code-sharing with scoreboard.html is BLOCKED by the build boundary (non-bundled HTML) and EXCLUDED by user. DEFERRED FEATURE (ledgered): GM embedded scoreboard mirror + cross-build shared-render module + scoreboard:page server-authoritative-page-model fork (mapping = workflow wf_c225b2b7-a7f). The minimal-consumer exact UX (hint text) → confirm at implementation per UX rule.

**PHASE 4a COMPLETE (2026-06-02).** P4a.1-.13 + the P0.4 reconciliation gap-task, all strict TDD. Backend (parent, direct): P4a.1 group:completed bonusPoints guard (14279e64), P4a.3+.4 sync:full +7 reconnect-restore required fields w/ UUID-fixture blocker fix (5c8bcb51), P4a.5 sync:request publish msg (6dacb393), P4a.7 device connectionStatus openapi+asyncapi+syncHelpers map (47dde044), P4a.9 lighting activeScene string guard (5404437c), P4a.11 subscribe oneOf += BatchAck/PlayerScan (3ab6c5c6), R2 fix (d9c7d530). Scanner (ALNScanner submodule d4c8a2c→12ca056): P4a.2 read bonusPoints (5b7da21), P4a.8 CC-8b comment (211e9b7), P4a.10 EnvironmentRenderer activeScene string (c0d88e8), P4a.12 forward+consume scoreboard:page+flip P0.4 (512fcc7), P4a.13 owner-sort recency parity (8726561) + formatCurrency de-dup (1e87edf), R1/R3 fixes (12ca056). **Parent HEAD 49d87d37.**

**P0.4 FLIPPED GREEN** (P4a.11 contract + P4a.12 scanner: MESSAGE_TYPES now == subscribe set, strict toEqual passes — NO subset/allowlist needed). P0.2 stays tracked-RED until P4d.11. P0.5 already green.

**4-lens boundary review (workflow wf_623303c2-0df) — 3 fixes + 4 ledgered NITs:**
- R1 (MEDIUM, phantom guard, I introduced): my P4a.8 CC-8b comment CLAIMED it pinned connectionStatus-reading, but the disconnected GM at gap-creating id GM_Station_5 made gap-fill return GM_Station_2 with OR without the filter → removing the filter still passed (verified). FIX: moved disconnected GM to SEQUENTIAL GM_Station_2 (with filter→_2 reuses freed slot; without→_3). VERIFIED RED when filter removed. LESSON: a "guard" test isn't a guard until you've watched it go red against the broken code — same anti-phantom discipline the whole plan is about.
- R2 (LOW, contract, I introduced by copying the plan's wrong schema): sync:full music.playlist declared [string,'null'] but buildMusicState emits musicService.getState().playlist = object|null. A loaded-playlist sync:full FAILED validation; CC-2 test had no musicService so never hit it. FIX: [object,'null'] + loaded-playlist regression test (VERIFIED fails against old type). LESSON: when "pinning the real shape," build a fixture that exercises the NON-default branch (loaded, not just null).
- R3 (LOW): scoreboard:page consumer tested only at app.js (EventTarget); networkedSession routing case untested (same pre-existing pattern as group:completed/player:scan). FIX: added networkedSession routing test.
- NITs ledgered P/Q/R/S (docs/plans/2026-06-02-alnscanner-comms-deferred-followups.md): P dead showGroupCompletionNotification (field mismatch, formatCurrency now masks the throw), Q getExposedOwners tie-break diverges from wall (benign), R tautological lighting assertion, S app.js:159 inline currency not formatCurrency.

**Verification:** ALNScanner full `npm test` = 1253 passed / 1 tracked-RED (P0.2) / 65 suites. Backend full = 1738 passed + 1 todo / 91 suites. ALNScanner coverage:check ✓ (no regression; new formatCurrency.js not yet ratcheted — regen gated on green suite/P0.2). **Backend coverage:check FAILS on 2 PRE-EXISTING env-specific files: vlcMprisService.js (78.9%) + displayDriver.js (79.5%) vs 80% branch threshold — both UNCHANGED on this branch (verified diff vs main empty), deterministic (same numbers ×2 runs), VLC/Chromium process-spawn branch coverage calibrated in a different env (CI). NOT a Phase 4a regression; would fail on main here too. Do NOT lower thresholds (CI calibration).**

NEXT: **Phase 4b — NFC robustness.** P4b.1 + P4b.2 ALREADY DONE (pulled into Phase 1). Only P4b.3 (null/non-string id guard, app.js processNFCRead:669), P4b.4 (drop url-record branch nfcHandler:129-138 + rewrite test :72-85), P4b.5 (log event.type @nfcHandler:76 + nfcReadErrorCount escalation @app.js:625-630, reset before findToken ~707) remain. P4b.5 soft-depends on P4b.3. Begins on user "proceed" per per-sub-phase cadence.

**PHASE 4b COMPLETE (2026-06-03).** NFC robustness, scanner-only. P4b.1/.2 were already done (pulled into Phase 1). ALNScanner 12ca056→ab17f51, parent HEAD d131c6db.
- P4b.3 (f2c549a): processNFCRead guards null/non-string result.id (NFC-4) — was only source==='error' guarded → result.id.length TypeError in un-awaited reading handler.
- P4b.4 (bdb014a): **CORRECTED SCOPE via user clarification** — plan said "drop url branch" but that falls to generic-decode which STILL queues junk Unknown (doesn't achieve NFC-5 goal). User clarified: production tags carry BOTH a text record (the token) AND a url record (unrelated). Restructured extractTokenId to PREFER first usable TEXT record regardless of record ORDER (fixed a LATENT bug: old first-match loop returned the url on a [url,text] tag, never reaching the text), and HARD-FAIL source:'error'/no-text-record when no usable text record (dropped url-record AND generic-decode token sources). LESSON: the plan's mechanical "drop the branch" didn't achieve its own stated goal; the user's clarification revealed the real requirement + a latent order bug.
- P4b.5 (93f9a3e): readingerror logs event.type (was [object Event]); nfcReadErrorCount escalates to Manual Entry hint after 3 consecutive errors; resets on usable read.

**3-lens boundary review (workflow wf_61749618-52f) — no HIGH/MEDIUM functional bugs. Fixes (ab17f51):**
- R1 (escalation scope, USER DECISION "escalate on both"): the Manual Entry escalation fired ONLY on hardware readingerror, NOT on P4b.4's logical no-text-record path → a misprovisioned tag tapped repeatedly never nudged to Manual Entry (missed NFC-6's "can't dead-end" goal). Routed all 3 failure surfaces (readingerror, source:'error', no-usable-id) through one `_recordNfcReadFailure()` helper sharing the counter; 3 bad taps of EITHER kind escalate. Added logical-escalation + reset tests.
- R2 (label, USER DECISION "drop (Debug)"): escalation hint said "use the Manual Entry button below" but button was "Manual Entry (Debug)" → dropped (Debug). e2e selectors use has-text/text= substring → unaffected.
- MEDIUM (test hygiene): removed 2 VESTIGIAL app-nfc-errors tests feeding source:'url-record'/'generic-decode' (unproducible after P4b.4; processNFCRead only branches on source==='error' so they passed for impossible inputs — phantom-mock smell) + updated stale 'unreadable-records' string → 'no-text-record'. (grep confirmed app.js:692 is the SOLE result.source consumer, never branches on the dropped labels.)
- NITs (not fixed, low/pre-existing): counter persists across team-switch (defensible: reader health is team-independent); error-result debounce keys on serialNumber so an immediate same-bad-tag re-tap within 2s is swallowed (pre-existing). Not ledgered separately — noted here.

**Verification:** ALNScanner full `npm test` = 1260 passed / 1 tracked-RED (P0.2) / 65 suites; coverage:check ✓ all 50 files. Backend untouched.

NEXT: **Phase 4c — Auth/HTTP/wizard hardening** (8 tasks, all scanner-only, all "ready" in pre-flight, no blockers). P4c.1 clear password field (AUTH-3); P4c.2 guard auth-response JSON parse + validate token (AUTH-4/HTTP-2); P4c.3 clear stale aln_auth_token on auth:required 3 sites (AUTH-5; NOTE a separate AUTH_INVALID branch @connectionManager:162-171 already removeItems but isn't one of the 3 targets); P4c.4 escape discovered-server URL via DOM API + drop dead server.ip (AUTH-6/HTTP-7); P4c.5 cap scan concurrency batch-32 + skip auto-scan when saved URL (HTTP-6; fixture needs #scanServersBtn + #connectionModal added); P4c.6 fetch tokens.json root-first (HTTP-3); P4c.7 guard tokens.json HTML-shell + validate non-empty map (HTTP-4; existing happy-path mocks need headers added); P4c.8 docs-only VideoController /api/videos (HTTP-8). Re-anchor all (app.js/connectionWizard.js shifted). Begins on user "proceed".

**PHASE 4c COMPLETE (2026-06-03).** Auth/HTTP/wizard hardening, scanner-only. ALNScanner ab17f51→5fe56d8, parent HEAD 9937563d. 8 tasks, all strict TDD: P4c.1 clear #gmPassword+autocomplete in finally (AUTH-3, 592338a); P4c.2 isolate authResponse.json() + validate token non-empty string before persist (AUTH-4/HTTP-2, 58f7e52); P4c.3 _clearStaleToken() at auth:required dispatches (AUTH-5, fc830c8); P4c.4 displayDiscoveredServers via createElement+setAttribute (XSS) + drop dead server.ip + selectServer guard (AUTH-6/HTTP-7, 353b8de); P4c.5 scanForServers BATCH_SIZE=32 + skip auto-scan when URL saved (HTTP-6, 5c3efb2; fixture needed #scanServersBtn+#connectionModal); P4c.6 tokens.json root-first (HTTP-3, 4bfae61); P4c.7 content-type + non-empty-map guard (HTTP-4, 03f34a2; happy-path mocks needed headers); P4c.8 docs-only /api/videos (HTTP-8, 1a2c267).

**3-lens boundary review (security/logic/test-quality, workflow wf_f789f33d-4ef) — fixes in 5fe56d8:**
- **R1 (MEDIUM, logic, I introduced via pattern-matching):** P4c.3 cleared a STILL-VALID token on the max_retries path. That path is reached ONLY for TRANSIENT failures (DEVICE_ID_COLLISION/transport drop) — auth failures throw earlier (connectionManager:163-172) and already clear. Wiping a valid token forced a re-type of the admin password to recover from a network blip mid-show (the reload-restore path uses the saved token). FIX: guard the max_retries clear with `if (!isTokenValid())` (mirrors the other 2 sites) so it clears only a token that actually expired during retries. Added test (maxRetries=1 + valid token → token preserved). LESSON (recurring): "think about FUNCTIONALITY not mechanics" — adding the clear at all 3 dispatch sites by pattern-matching the plan was wrong; max_retries ≠ bad-token.
- **R2 (MEDIUM, test hygiene, phantom):** the HTTP-4 SPA-shell test passed even WITHOUT the content-type guard (its json() rejected → outer catch → same false outcome). FIX: make json() RESOLVE a non-empty object so ONLY the content-type guard produces false. VERIFIED via neutralize-the-guard revert-check (test fails without guard, passes with). Anti-phantom discipline again.
- **R3 (LOW, security, I predicted):** AUTH-3 password-clearing finally was attached to a try that began AFTER the field-validation early-return → a typed secret persisted when serverUrl/deviceId missing (deviceId empty when /api/state unreachable). FIX: moved validation INSIDE the try (single clearing site). Extended the existing early-return test to assert password cleared.
- R4/R5 (nits): tightened concurrency assertion <=32 (was <=64); made the server.ip test discriminating (pass ip+url, assert url used).
- Review CONFIRMED-CLEAN: XSS fully closed (setAttribute, no innerHTML sink; dataset.arg round-trips as plain string); token validation sound; batching covers all 509 candidates no off-by-one; non-empty-map guard can't reject a 1-token DB nor accept array/null; 10/12 new assertions verified to FAIL against pre-fix code (genuinely discriminating).

**Verification:** ALNScanner full `npm test` = 1274 passed / 1 tracked-RED (P0.2) / 65 suites; coverage:check ✓ all 50 files. Backend untouched.

NEXT: **Phase 4d — Renderer/state correctness & misc** (11 tasks, .10 removed → P4d.1-.9, .11, .12; scanner-only except P4d.9 which is a backend asyncapi-prose change). **P4d.11 deletes dead restartSystem/clearData → flips P0.2 GREEN** (the last tracked-RED). Pre-flight flags: P4d.9 cited contract-test file is HTTP-only — relocate the prose-assertion to a yaml-loading test (BLOCKER); P4d.11 stale deletion target — AdminOperations.test.js now has `describe('non-contract emergency methods (AC-4)')` @31-48 (NOT the plan's named describe blocks) whose tests CALL the methods → remove THAT block. P4d.3/.5 require rewriting existing tests; P4d.6 jsdom CSS-undefined caveat. Begins on user "proceed".

**PHASE 4d COMPLETE (2026-06-03) — FINAL SUB-PHASE; comms-fixes plan implementation DONE.** Renderer/state correctness + misc, scanner-only except P4d.9 (backend asyncapi prose). ALNScanner 5fe56d8→b2647ce, parent bump 0c96e59e (backend AC-5 prose committed directly @fcd62547). 11 tasks (.10 removed), strict TDD:
- P4d.1 SR-2 escapeHtml session name (SessionRenderer, all 5 template sites; differential path already textContent-safe).
- P4d.2/.3 SSR-2/SR-3: StateStore.replace() full-domain swap (drops orphaned keys, shallow-equal skip, listener error-isolation); sync:full 10 domains switched update()→replace() so stale keys don't survive reconnect restore. service:state stays update() (merge).
- P4d.4 SSR-3 structuredClone deep-copy in get()/getAll() (+ JSON-clone polyfill in test-setup.js for jest sandbox).
- P4d.5 SR-4 removed dead discoveredDevices merge/Pair branch (EnvironmentRenderer); device:discovered isn't even in MESSAGE_TYPES.
- P4d.6 SR-5 escapeCssAttrValue helper (new util) for querySelector id lookups (Health/Cue/Env) + escapeHtml on the matching attribute writes.
- P4d.7 WS-5 socket:connected dispatched ONLY by persistent on('connect') (removed per-connect resolver dispatch → no double-fire on reconnect churn).
- P4d.8 WS-7 warn on non-conforming (un-enveloped) forwarded events; `||`→`??` narrows fallthrough to null/undefined only.
- P4d.9 AC-5 backend asyncapi prose: audio:route:set sink accepts `'hdmi'|'bluetooth'|<pipewire-sink-name>` + relocated prose-assert to a yaml-loading contract test (HTTP-only file was the pre-flight blocker).
- P4d.11 AC-1/CC-6 DELETED dead AdminOperations.restartSystem()/clearData() (emitted system:restart/system:clear, never in enum) → **flipped P0.2 gmCommandActionConformance GREEN — the last tracked-RED retired.** Removed the AC-4 describe block that CALLED them (pre-flight catch — not the plan's named blocks).
- P4d.12 WS-6 interim: serialize same-action gm:commands client-side via `_actionChains` (acks correlate only by action name → two in-flight same-action would cross-resolve). First command sends synchronously; same-action followers chain on prior.settle. Full per-command requestId correlation deferred (coordinated contract change).

**3-lens boundary review (logic/security/test-quality, workflow wn68rhf94/wf_ccfc4014-a88) — UNANIMOUS: correct, security-sound, test-CLEAN (every new test empirically revert-verified to discriminate; zero phantom mocks). Fixes in b2647ce:**
- **WS-6 LOW-1 (logic):** chained `_sendCommandOnce()` didn't re-check the connected guard → raw `TypeError: Cannot read properties of null (reading 'on')` (not clean "Socket not connected") if socket torn down between first + chained same-action command. Exactly the reconnect-churn this plan targets. FIX: guard at top of `_sendCommandOnce` + inner cleanup `this.socket?.off`. RED-verified (threw the exact null TypeError).
- **WS-6 LOW-2 (logic):** `_actionChains` never reset in `_cleanup()` → post-reconnect same-action command queues up to ~10s behind a doomed pre-reconnect command before sending. FIX: `_cleanup()` resets `_actionChains={}`. RED-verified (chain stayed set after cleanup).
- **HealthRenderer NITs (security, completes P4d.6 pass):** escape backend-controlled `${s.status}` (real input path via health map; server enum-validation is primary guard) + `data-service-id` (consistency w/ sibling data-service; s.id is fixed literal set, no live path). status fix RED-verified (rendered `<img>` raw).
- **Comment hygiene:** dropped stale "EXPECTED RED" docstring in gmCommandActionConformance (permanently green now); documented structuredClone JSON-serializable equivalence caveat.
- **Ledgered (deferred-followups doc, entries T/U):** WS-6 full requestId correlation (interim by design); the sibling CommandSender.sendCommand() admin-command path has the same by-action cross-resolution risk and is NOT covered (review-noted as out-of-scope; commit scopes WS-6 to the teamRegistry session:addTeam path via orchestratorClient).

**Verification:** ALNScanner full `npm test` = **1290 passed / 0 RED / 65 suites** (all 3 Phase-0 tracked-REDs P0.2/P0.4/P0.5 RETIRED); coverage:check ✓ all 50 files. Backend unchanged this round (was 1739+1 todo / 92 suites green); AC-5 prose lives at parent fcd62547.

**=== COMMS-FIXES PLAN IMPLEMENTATION COMPLETE ===** All phases 0–4 done. Remaining before merge: pre-merge full verification (backend integration ~5min sequential + E2E with dist rebuild) — user's call. Deferred-ledger items (P–U + scoreboard-unification feature) are all non-blocking, tracked in docs/plans/2026-06-02-alnscanner-comms-deferred-followups.md.

**E2E REMEDIATION (2026-06-03) — pre-merge L3 alignment + new coverage.** The comms-fixes plan deferred E2E to a pre-merge gate over the UNCHANGED suites. Before running blind (which would've been a 30-min timeout-storm), mapped each behavioral delta → affected L3 artifact (4-cluster workflow wah2u4n2i) and remediated. Parent commit 1cd5fd98 (backend test code, direct in parent — NOT a submodule). All validated on the Pi (mobile-chrome project, dynamic ports).
- Tier A (suite-validity): GMScannerPage.startGame() fixed-500ms → sessionActive.waitFor() (the new processNFCRead session-active gate made the 500ms a latent race); manualConnect/waitForConnection RL-7 decoupling (assignStationName no longer falls back to GM_Station_N → wait for settle + force-set deviceId + clear stale ❌); assertSyncFullStructure +8 required fields (silent-desync guard); assertTransactionResultStructure enum += queued,rejected + clientTxId; deleted dead disconnectWebSocket/reconnectWebSocket (removed-globals no-ops) → replaced with waitForDisconnected/waitForReconnected (#connectionStatus class waits).
- Tier B (new coverage): B1 (07d-02) pause→scan gate-block + resume→succeed + expectScanBlocked() helper. B2 (07b) durable queue via Playwright routeWebSocket transient-drop (transparent connectToServer proxy + online-flag gating) — scan during drop queues, flushes once on auto-reconnect.
- **Pre-flight service inventory was MANDATORY** (user insistence): no competing orchestrator/PM2/VLC, but an orphaned scoreboard kiosk Chromium was wedged on a dead :3000 (killed it); PipeWire/HA/BT healthy, MPD absent (spawns per test). The L3 harness spins a REAL orchestrator (real VLC/audio/BT/MPD/Chromium) on the deployment Pi — outward-facing side effects; confirm the Pi is free before running. The NARROW flows (07b/07d-02/00-smoke) don't exercise audio/BT/lighting/video so are low-sensitivity; the FULL suite (07d-03/04, 08, 22/25, 23/24/27) is highly service-sensitive + disruptive (real BT scans, audio routing, HEVC on the TV).
- **Findings (ledgered in deferred-followups V + test-infra lessons):** GM connectionManager gives up auto-reconnect after maxRetries=5 (~16s) → auto-reconnects across a transient blip but NOT across a backend restart (Pi restart ~10-20s > budget); JWT stable across restart (constant default secret). Playwright setOffline does NOT drop an established socket.io WS (use routeWebSocket). 2 new L3 tests added to baselines.
- **Validation:** B1 ✓, B2 ✓ ($25k single-flush no-double), 00-smoke-test 14 ✓ (A3 against real sync:full), L2 scanner suite 50 ✓ (tokenManager content-type fine in Vite + Manual Entry label + wizard + standalone). flow 24 untouched (added then reverted). A4 helper has no callers (inspection-correct). FULL L3 suite NOT yet run (needs Pi free + all-services-healthy; the service-sensitive flows are unvalidated against the comms changes).

**FULL L3 SUITE TRIAGE + DUCKING-GATE FIX (2026-06-03).** First full L3 run (after the E2E remediation): **136 passed / 49 skipped / 5 failed (3 distinct tests)**. CRITICAL: `npm run test:e2e 2>&1 | tee log` → exit 0 is from `tee`, NOT playwright; ALWAYS grep the summary line for failures.
- **3-lens deep-dive (workflow wyrap75ov) — NONE of the 3 failures is a comms-fixes regression** (all 3 test files unchanged by comms-fixes; verified `git diff main..HEAD` empty):
  1. **07d-04 "Music auto-ducks when video plays" = pre-existing PRODUCT-CONFIG bug.** `broadcasts.js` gated the video/sound→`audioRoutingService.handleDuckingEvent()`/`applyRouting()` wiring behind `NODE_ENV !== 'test'` (commit b48ad0f5, 2026-05-23, ancestor of BOTH main+HEAD → fails on main too). The E2E orchestrator ALSO runs NODE_ENV=test, so the wire 07d-04 drives end-to-end (video:queue:add) was DEAD — **ducking was unexercised at E2E since 2026-05-23.** MPD/BT are red herrings (ducking arms regardless of a live music sink; `_setVolumeForDucking` failure is swallowed).
  2. **admin-state-reactivity "Video State GM1→GM2" = env-timing flake.** `#now-showing-icon` ▶️ assertion used the 5s expect default + global 60s test budget — too tight for 2-GM setup + VLC loading→playing under load. (raw `browser.newContext` GM2 is NOT the cause — cue-sibling uses it and passed.)
  3. **30-full-game video-compound cue = env-timing flake.** A momentary vlc-health flap under load HELD the cue (service_down); a held cue auto-cancels (never active) so the 5s `waitForActiveCue` timed out, and `fireCue` returns success even when held (masks it). `waitForVideoIdle` (now-showing==='Idle Loop') doesn't cover the health path.
- **FIX (commit 93a8b4f8):** ducking gate → `ENABLE_AUDIO_WIRES !== 'false'`; `jest.config.base.js` sets it 'false' (jest off, provably identical to the old NODE_ENV gate — the ex-stub files cue-engine/video-orchestration/service-state-push stay pactl-free), E2E spawned orchestrator + prod leave it unset → wire ON. `broadcasts.test.js` ducking-wire unit tests now enable via the flag (not NODE_ENV='production'). Hardened all 3 timing-flaky tests (test.setTimeout bumps + explicit assertion timeouts + a pre-trigger `waitForBackendState(serviceHealth.vlc.status==='healthy')` for both 30-full-game AND 07d-04 ducking — video:queue:add/cue is rejected/held when vlc flaps down).
- **VERIFIED:** unit+contract 1739 green; the 4 audio-wire integration files green (44); **07d-04 ducking now PASSES on chromium** ("Music ducked by: video" + duck-off) → gate fix confirmed end-to-end; 08/22/25 video flows unaffected by the newly-enabled wire (22 retry-flaked once). The mobile-chrome ducking attempt flaked under late-in-run load (VLC slow to start → no video:started) → that's WHY the ducking test was also hardened.
- **TEST-INFRA LESSONS (recurring, for future L3 disconnect/video work):** (1) Playwright `setOffline(true)` does NOT drop an established socket.io WS — use `page.routeWebSocket(/\/socket\.io\//, ws => online ? ws.connectToServer() : ws.close())` (connectToServer w/ no onMessage = transparent proxy, socket.io heartbeat passes through). (2) orchestrator stop/restart = deterministic disconnect BUT the GM scanner gives up auto-reconnect after maxRetries=5 (~16s) — can't auto-reconnect across a ~10-20s Pi restart (JWT IS stable across restart; the blocker is the give-up). (3) The E2E orchestrator runs NODE_ENV=test like jest — never use NODE_ENV to gate E2E-vs-jest behavior; use a dedicated flag set only by jest.config.base.js. (4) Per-test orchestrator restarts respawn MPD/VLC → late-in-run tests hit the most CPU contention → video-timing flakes cluster at suite end.

**CORRECTION — admin-state-reactivity root cause (2026-06-03).** The "Video State GM1→GM2" failure was MIS-diagnosed by the deep-dive AND my first fix as "VLC loading→playing latency" (env-timing). WRONG. Confirmed via isolated run (deterministic fail, both projects) + trace analysis (unfinished `evaluateExpression`) + a fix-experiment: the real cause is line 76 `await page1.evaluate(async (url)=>{ const resp=await fetch(`${url}/api/state`); return resp.json(); })` — a raw IN-PAGE fetch with NO timeout that hangs (status=-1, aborted only at teardown). It hung in SETUP (test never logged "GM1 queuing video"). `initializeGMScannerWithMode` succeeded just before (its wizard /api/state fetch worked), so the hang is specific to the post-init in-page evaluate(fetch). NOT a comms-fixes regression: the GM-scanner SW bypasses /api/ in BOTH old (338900301:sw.js) and the comms-fixes-rewritten sw.js (`isBypass` = pathname startsWith '/api/'|'/socket.io/' → fetch handler returns early) — /api path unchanged → fails on main too. FIX (commit 1bab16e2): use `gm1.getStateFromBackend()` (Playwright page.request — bypasses the page SW, established suite pattern) for the health check → test reaches the video-queue step and passes ~15s isolated. Reduced my over-bumped test.setTimeout 120s→90s. **LESSON: a raw `page.evaluate(fetch(...))` with no timeout/AbortSignal can hang indefinitely (no Playwright action-timeout governs it → surfaces as a generic "Test timeout" at the finally's context.close, NOT an assertion error). Always use page.request (getStateFromBackend) for backend fetches in E2E — it bypasses the page SW and has built-in handling. The deep-dive's "likely-pass in isolation" prediction was WRONG; always verify a flake hypothesis with an isolated run before believing it.**

# Red team — doctrine faithfulness and cross-repo parity

Plan under attack: `docs/plans/2026-09-12-block2-hardening-plan.md`. Tree: `claude/nice-curie-hescfv`.
**The plan moved during this review**: my first read was at `727a68d`; three commits later HEAD is
`145c05d` (R5 ruled "every connection presents a credential", P15/T3b now mandatory, R6 shipped-enabled,
§8 reworded). Findings below are against `145c05d`. The plan header still cites census tree `e87f8c5`.
Read-only; nothing in the tree was touched.

## 1. Findings

| id | severity | plan pin or task | objection | evidence | fix |
|---|---|---|---|---|---|
| D-1 | BLOCKING | P10 (Chromium clause), P12, T3 | P10 puts Chromium under ProcessMonitor supervision "with the same strategy table" and rules the display is NOT a ninth registry service, while the escalation rule reads "on gave-up the OWNING SERVICE reports down". Chromium has no owning service: `KNOWN_SERVICES` is the eight, there is no `display` service domain, and P12's `service:restart {serviceId}` enumerates only those eight. So a kiosk that flaps to gave-up has no red row and no Restart verb anywhere — R-C3-3 ("escalation to a red row with a Restart verb only on exhaustion/flapping") and CONTEXT §4 "Status with verbs" are unmet for exactly the process ROADMAP §5 names ("scoreboard liveness as a first-class check", Block 2). The T4 preflight row is on-demand paper/live at preflight time, not a mid-show escalation surface. The "required eight stand" is this plan's own §6 constraint, not a ratified ruling (M6 re-pinned the endpoint interior; nothing in §8 freezes the registry count). | `backend/src/services/serviceHealthRegistry.js:14`; `backend/contracts/asyncapi.yaml:2344` (10 domains, no display); `backend/src/websocket/broadcasts.js:495-591`; `backend/src/utils/displayDriver.js:147,174-175` (spawn + exit warn, no registry report); spec §8 R-C3-3; ROADMAP §5 table row "Is it working"; CONTEXT §4 "Status with verbs" | Make `display` the ninth registry service, contract-first (add to `DomainStateHealth.required`, the three enum sites already being edited, the scanner's `SERVICE_NAMES`, `capabilities.js` keys), owned by `displayDriver` (healthy = kiosk process alive + observe socket connected), `service:restart display` = relaunch; dormant via `display.main` absent (P1 already maps it). If the owner keeps eight, then P10 must say Chromium is NOT supervised in Block 2 and the display row is preflight-only — recorded as a deliberate gap against R-C3-3. |
| D-2 | BLOCKING | P5, P8, T1a, T4 (`resolution.js` scope) | `resolution.js` `case 'service'` faults every live status that is not the literal `'healthy'`. P5 introduces `'dormant'` in the registry and P8/T4 feed "services from the registry incl. dormant" into the resolver's inventory, so every dormant service resolves `fault` at live depth, lands in `rollup.problems`, and the preflight's service arm shows red for intentionally absent equipment — the always-red the unit exists to kill (CONTEXT §4 alarm integrity). T1a's `resolution.js` scope names only "P2 dependent-need rule; endpoint `installed:false`"; no dormant branch in the service case. | `backend/src/gameRules/resolution.js:135-150` (`health === 'healthy'` else `fault`), `:187-189` (problems); plan §4 T1a file list; spec §5 M8 (every consumer sweep) | T1a red-first: `inventory.serviceHealth[id] === 'dormant'` → verdict `dormant`, depth `live`, reason from the registry's `door`; rollup counts it under `dormantServices`, never `problems`. Pin end-to-end: toy dormant profile → feed → `preflightService.evaluate({live:true})` → the `lighting` row is `dormant`, `problems` empty. |
| D-3 | BLOCKING | P1, T1b (`simulationProfile.js`, `toy-test-rig.json`), T7 gates | P1 keys dormancy on the PROFILE alone ("absent family ⇒ dormant"; `audio.sinks → audio,sound,music`; the `btAddress` rule; T1b's own test "a profile that omits `lighting.instruments` yields lighting dormant"). But the generated rung-1/E2E simulation profile declares only the endpoint families the pack MANIFEST names as needs — for ALN that is `display.main` alone, for the toy nothing — and every Tier L run without `E2E_PROFILE_PATH` uses that generated profile. After T1a+T1b: rung 1 has lighting/audio/sound/music/bluetooth dormant (the 13-assertion audit expects 7 healthy and flips a real witness light through `lighting`), and both existing Tier L legs boot with most capabilities false → primary-path tests skip loudly and the dual-pack gate goes hollow-green while `requireDegraded` flows fail. T1b's generator line says only "declare the pinned interior shape for generated endpoints" (reshape, not add), and the toy-test-rig edit says only "declare lighting installed". The design's own rung doctrine: "On rung 1 … resolve() must find nothing dormant." | `backend/scripts/lib/simulationProfile.js:9-11,35-40`; `backend/tests/e2e/setup/session-env.js:122,154` (absent pin → generated profile); `backend/tests/rung1/audit-flows.js:89-95`; `ALN-TokenData/pack-manifest.json` `hardware.endpoints` = `display.main` only; toy manifest `endpoints: {}`; plan §3 P1, §4 T1b | Decide the semantics and write it into P1: EITHER (a) dormancy is keyed on manifest-declared endpoint needs (C1 §2 row 3's wording: "endpoint `onAbsent: degrade` + not installed"), with profile-only absence of an undeclared family a paper `unknown`, OR (b) profile-alone as drafted, in which case T1b's generator must declare every family the harness provides (`display.main`, `audio.sinks` with the null-sink ids, `lighting.instruments`, `stations {count:0}`, `personal {expected:false}`) and `toy-test-rig.json` must declare all provided families, not just lighting. Pin: `dormantServicesFor(generateSimulationProfile(alnNeeds))` and `(toyTestRig)` are both empty; rung-1 audit unchanged at 13/13. |
| D-4 | MAJOR | P3 (compound cues) | P3 keeps a mixed cue enabled and refuses each dormant command "quietly" at fire. For a COMPOUND cue whose video entry depends on a dormant `vlc`, the timeline runtime sets `driveMode='boundary'` BEFORE executing the video entry and the refusal path `continue`s with the cue still in boundary pause waiting for a `video:progress` that will never come; post-video entries never fire, the cue stays in `_timeline`, and every later fire is refused as "already running". Under P3 this is an enabled cue that hangs on first fire — worse than a disable. | `backend/src/services/cue/timelineRuntime.js:233-238` (boundary set pre-execute), `:249-253` (refusal `continue`s); `backend/src/services/cueEngineService.js:519-522` (re-fire refused); `:421-424` (only `video:progress` leaves boundary) | P3 gains: a cue whose TIMELINE contains an entry on a dormant service is dormancy-disabled whole (compound cues are not "mixed" for this purpose); or, if the owner wants the mixed rule for timelines, the refused video entry must transition to `post-video` with `videoDuration = 0` and resume clock drive. Pin with a unit test on a compound cue under dormant vlc. |
| D-5 | MAJOR | P3, P4 | P3 narrows D-C3.2's "cues whose commands need dormant services" and C1 §2 row 3's "dependent cues … disabled at session start" to WHOLLY dormant cues, arguing from "degrade, never kill" — a phrase that appears in no ratified text (its only prior use is the slice-4 doc's boot-survival rule). The S2 adjudication reserved command-level refusal for UNBOUND ROLES and kept "cue-level disables … the dormant-SERVICE mechanism only"; P3 makes dormant-service handling command-level for mixed cues without recording the deviation. And the mixed cue's summary carries nothing (P4's `disabledBy` is only for wholly-disabled cues): the GM sees an enabled cue, fires it, the lighting half silently does not happen, info log only — "verdicts appear at the moment of choice" (CONTEXT §2, the designer's/GM's loop) is not honored on the fire button. | spec §3 D-C3.2 (session-start disables), §5 S2 adjudication; C1 §2 row 3; plan §3 P3 line "degrade, never kill"; `grep -rn "never kill" CONTEXT.md docs/plans` → only `2026-08-29-phase3-a3-slice4-showcontrol-pack.md:231,754`; CONTEXT §2 "One truth, three loops" | Record P3 as a deliberate refinement of D-C3.2 justified by S2's own reasoning (cue-level disable kills the cue's other commands), owner-visible at checkpoint 1. Add to cue summaries `dormantCommands: [{action, service, door}]` and badge the quick-fire button ("lighting not installed tonight"); keep the fire-time refusal quiet. Drop the invented phrase. |
| D-6 | MAJOR | §7 stop points (T1a "Block 2a candidate"), T1a pre-registration | T1a pre-registers `service:restart`, `service:out-of-service`, `service:in-service`, `preflight:run` in the asyncapi action enum "with no switch case yet", and contracts `DomainStatePreflight` with no producer until T4. On the T1a tree (the plan's named merge candidate) the executor answers those actions "Unknown action" — the contract advertises four commands the server refuses, and a domain nothing emits. That is the contract-truth defect class the fix vehicle's §2.2 sweep exists to remove (P4-3 "newly gated but OpenAPI still documents it as open"). | `backend/src/services/commandExecutor.js:862-867`; plan §4 preamble ("T1a pre-registers every new action name in the contract enum…") and §7 T1a row; fix vehicle §2.2 | Contract enum entries and `DomainStatePreflight` ride the task that implements them (T3, T4). T1a pre-registers only code-side tables (`REQUIRED_PAYLOAD_FIELDS`; the `service:` prefix is ALREADY floored at `grants.js:80`, so no floor edit is needed for `service:*`; `preflight:` needs one — see D-17). |
| D-7 | MAJOR | §7 T3/T5 stop points; Method ("a cross-repo parity change is one task holding both sides") | T3 ships the escalation message "crashed N times in Ws — supervision stopped; Restart to try again" and the three `service:*` commands, but the Restart / Run-without-it / Put-back buttons land in T5 — §7 calls the T3 tree "coherent; the GM sees their buttons only after T5". By CONTEXT §4 that tree is "a dashboard, not a control panel": a fault row that names a verb the GM cannot press. The verbs are a cross-repo parity change split across two tasks, against the plan's own method rule; and §7 lists T5 as depending on T1a only while §4 says it "runs with T3, T4" — T5's buttons need T3's switch cases. | plan §3 P10 (message text), §4 T3/T5, §7 rows T3/T5; CONTEXT §4 "Status with verbs" | T3 holds both sides of the verbs (backend cases + `HealthRenderer` verb buttons + `AdminOperations` wiring + dist); T5 keeps self-heal + the §8.5 tests. Then T3 is a real stop point and T5 truly depends on T1a only. |
| D-8 | MAJOR | §5 DoD (g), T4 | (g) "The CLI's JSON and the panel's rows agree for the same tree" cannot hold as written: T4 states the CLI "without a running engine" reports the service and display arms `unknown`, while the panel is fed by the live engine's evaluation. The two disagree by construction on every live row. | plan §4 T4 ("scripts/preflight.js … without a running engine the service and display arms report `unknown`") vs §5 (g) | Restate (g): paper rows agree byte-for-byte; live rows agree only when the CLI attaches (`--engine <url>` hitting `preflight:run` or `/api/state`) — or scope (g) to the paper arms and say so. |
| D-9 | MAJOR | Scope inventory (§7) vs fix vehicle §2.1 | F-P2-3 (the two manifest builders diverge on symlinks; a Python-built inventory serves a symlinked file from OUTSIDE the pack dir through `/api/pack/files/`, containment checked by nominal path only) is on Block 2's scope list by ratified deferral ("hereby ON that scope list") and root-caused in the census, and the plan never mentions it — no task, no deferral reason. | `docs/plans/2026-09-05-train-fix-vehicle.md:154-156`; census `2026-09-12-block2-reopen-census.md:443` (root cause: `build-pack-manifest.js:51-69` Dirent walk excludes symlinks; `build_pack_manifest.py:65-79` follows them; `packService.js:1450-1460` no `realpathSync`); `grep -n "F-P2-3\|symlink" docs/plans/2026-09-12-block2-hardening-plan.md` → none | Add to T4 (its pack-integrity arm already walks the manifest against disk): `resolvePackFile` compares `fs.realpathSync` to the pack dir; both builders skip symlinks (or both follow, but then containment applies to the target); a red-first test with a symlink escaping the pack dir. Or record the deferral with a reason and a home. |
| D-10 | MAJOR | T4 devices arm vs S3 / C1 §3 groups 5–6 | The S3 adjudication ruled the devices arm IN with "named sinks present"; C1 §3 group 6 says "audio sinks present (named BT speakers connected)"; CONTEXT §5's own example of a live check is "the sink exists in pactl right now"; T1b's interior declares sinks by `id`/`btAddress` precisely so this can be checked. T4's devices arm lists only staffed count and stations `unknown`. Group 5's "every consumer reports the ACTIVE pack version/hash" also has no row, although each GM station's `packHash` is already recorded on its socket. | spec §5 S3; C1 doc §3 items 5–6; CONTEXT §5 "Paper vs live checks"; plan §4 T4 devices arm; `backend/src/websocket/socketServer.js:127,139-144` | T4 adds rows: `sinks.<id>` (paper: declared; live: `pactl list sinks` / BT connected for `btAddress` sinks; `unknown` when `audio` is down or dormant per M7) and `stations.pack` (connected GM sockets whose `packHash` ≠ active hash, live). Keep stations-synced `unknown` with its recorded reason. |
| D-11 | MAJOR | P8, T4, T6 vs ROADMAP Q13 honesty rule | ROADMAP §4 Block 2 (ratified Q13): "the preflight is not one instrument and states its own limits ON ITS FACE … physical tap-through belongs to the human checklist". The plan delivers the paper/live labels (P8) but no limits statement in the evaluation shape, the panel (T6), or the CLI (T4) — half the ratified deliverable is missing. | ROADMAP §4 "Block 2 … The honesty rule (ratified, Q13)"; plan §3 P8 shape `{profileId, forPack, packHash, computedAt, depth, rows, rollup}`; §4 T4/T6 | P8's result carries a fixed `limits` block (`verifies: software chain …; cannotVerify: physical tap-through, room …; humanChecklist: docs/preflight-checklist.md#…`); T6 renders it as the panel footer, T4's CLI prints it as the header; the checklist doc's absorbed sections point at row ids, its retained sections are the `cannotVerify` list. |
| D-12 | MAJOR | P8 row verdicts vs "one truth" | P8 rows carry `verdict: go|warn|no-go|dormant|unknown`; `resolve()` emits `runs|dormant|fault|no-go` (+ `unknown` per M7) and a rollup `go|go-degraded|no-go`. Where `fault` lands is unstated (`warn` softens faults; `no-go` would make a down service refuse start against C1 §2 row 1). CONTEXT §2: "every surface … quotes those verdicts rather than forming its own opinion" — a translation layer is an opinion. | `backend/src/gameRules/resolution.js:55-166,191-198`; plan §3 P8; CONTEXT §2 "One truth, three loops" | Rows quote `resolve()`'s verdict verbatim and add a separate `severity` field for the R8 class (certificate = `warn`); the rollup is `resolve()`'s. Preflight-only arms (host, network, display) emit needs into the same vocabulary. |
| D-13 | MAJOR | P14 vs R-C2-1 | P14 heals only "when orchestrator-served". `packLoader.channel()` fetches `/api/pack/*` only at `/gm-scanner/`; every other origin fetches its own static bundle. A GitHub-Pages-served scanner in networked mode (a documented deployment: GitHub Pages auto-deploy + the connection wizard's orchestrator URL) would `refresh()` its OWN bundle, still mismatch, and hit the blocking backstop on every pack change — R-C2-1 says "fetch the CONNECTED orchestrator's current pack … each orchestrator is authoritative for its own connected stations". CORS already admits RFC1918/`.local` origins and `packRoutes` is unauthenticated, so the cross-origin fetch is available. | `ALNScanner/src/core/packLoader.js:104-119`; `backend/src/app.js:60-68`; `backend/src/routes/packRoutes.js:19,34`; spec §8 R-C2-1; ALNScanner/CLAUDE.md "Deployment" | `refresh({baseUrl})` where `baseUrl` is the connected orchestrator's URL from `connectionManager`, regardless of serving origin; the same-origin static channel stays the un-networked default. Test: Pages-origin pathname + orchestrator URL → refresh hits `<orch>/api/pack/manifest`. |
| D-14 | MAJOR | T1a (`capabilities.js`) on the dormant leg | `requireDegraded()` gates the OPPOSITE way — it RUNS when a listed capability is absent — and the flow at `admin-state-reactivity.test.js:194` fires a cue and asserts a `held-cue-*` item appears. On T1b's dormant-lighting leg `caps.lighting === false` (T1a: "caps[key] = false, caps.dormant[key] = true"), so the degradation test runs and fails by design (dormant refuses without a hold — P3/M5). T1a names `capabilities.js` but not `requireDegraded`; the third leg is red on arrival. | `backend/tests/e2e/helpers/capabilities.js:87-95`; `backend/tests/e2e/flows/admin-state-reactivity.test.js:193-206`; spec §5 S5 | `requireDegraded` treats only DOWN as degraded (`caps[k] === false && !caps.dormant[k]`); add `requireDormant(test, caps, keys)` for the S6 flow; pin both on the dormant leg. |
| D-15 | MINOR | T1a ‖ T1b | T1a "consumes from T1b" `dormantServicesFor`, `ENDPOINT_FAMILIES` and the dormant fixture profile, yet the two run in parallel worktrees on separate branches; T1a's dormancyService tests cannot import a module T1b has not merged. | plan §4 table ("Runs with") and T1a "Consumes from T1b" | T1b first (it is small), or the interface + fixture paths pinned in T1a's brief with a stub the merge replaces. |
| D-16 | MINOR | P9 | Adding a `preflight` domain is not a new discrete event (constraint honored), but it changes the domain enum, the contract test's domain list, CONTEXT §4's "10 domains", Block 3's ROADMAP charter ("10 service-state domains … 10 restore guards"), and the scanner's restore mapping — none named for update beyond "CONTEXT.md if a term sharpened". The same evaluation also has three carriers (`session.metadata.preflight`, `sync:full.preflight`, the domain) with no named source of truth. | `backend/contracts/asyncapi.yaml:2344,2354`; `backend/tests/contract/websocket/service-domain-state.test.js:69`; CONTEXT §4 "Service domain"; ROADMAP §4 Block 3; `ALNScanner/src/network/messageRouters.js:128-134` | Name the one source (the last evaluation held by `preflightService`), derive the other two; list the enum/test/CONTEXT/ROADMAP edits in T1a/T4; add a `domain-modeling` row: "service domain" now includes a verdict sheet. |
| D-17 | MINOR | T1a floor map, `preflight:run` | `FLOOR_ACTION_PREFIXES` already floors `service:` (so T1a's "pre-register in the floor map" is a no-op for the three verbs) but has no `preflight:` prefix; non-floor gm:commands are reachable from observe sockets (the B0 close-review comment), so an observe token could trigger live probes. | `backend/src/gameRules/grants.js:63-83` | Add `['preflight:', 'show-control']` explicitly in T1a; say the `service:` verbs inherit the existing row. |
| D-18 | MINOR | P5/P13, T1a tree wording | On the T1a tree a dormant `vlc` already refuses the player-scan video without a hold (`canAcceptVideo` → `!isHealthy` → 409) — good — but the wire message reads "VLC is offline: <msg>" for dormant, and `service:check` on a dormant id runs the probe (whose `report()` P5 ignores) and acks "= down". Both are M8's "commandExecutor wording" class. | `backend/src/services/videoQueueService.js:866-871`; `backend/src/routes/scanRoutes.js:96-132`; `backend/src/services/commandExecutor.js:820-832` | T1a: `canAcceptVideo` returns `reason:'vlc_dormant', message:'video not installed tonight'`; `service:check` short-circuits dormant ids with "not installed tonight". Keep P13's `processQueue` branch in T3 (only reachable via the operator door). |
| D-19 | MINOR | T1a parity list | Two enum consumers in the census are absent from T1a's file list: `backend/tests/e2e/helpers/assertions.js:187` (`expect(['healthy','down'])`) and `backend/tests/rung1/audit-flows.js:89-95` (T3 adds an assertion; T1a's `btAddress` rule turns rung-1 bluetooth from `down` to `dormant`, which the audit should record, not ignore). | census §1 table; plan §4 T1a | Add both to T1a. |
| D-20 | MINOR | DoD (b), T1a dormancyService test, T1b | "a mixed cue is not [disabled]" — the toy pack has no mixed cue (one lighting-only, one sound-only cue) and no task edits `toy-heist/cues.json`. | `backend/tests/e2e/fixtures/packs/toy-heist/cues.json` | T1b adds a mixed cue (sound + lighting role) to the toy pack (the manifest is being regenerated anyway) and a compound one if D-4's rule is adopted. |
| D-21 | MINOR | P3, executor ordering | `_resolveLightingRole` runs BEFORE the health gate, so every dormant refusal of a fallback-bearing role fires the L7 loud warn first (the toy's `vault-alarm` has a fallback). Noise on a by-design path. | `backend/src/services/commandExecutor.js:151-170`; toy `game.json` `lightingRoleFallbacks` | Check dormancy before role normalization (or suppress the L7 warn when the gate will refuse). |
| D-22 | MINOR | P7 | The depth of the session-start evaluation is unstated (paper vs live). R-C3-1 keys the gate on `require` needs "unresolved" = `resolve()`'s `no-go`; a live-DOWN required service is a fault with verbs (C1 §2 row 1), not a no-go, and must NOT refuse start. | spec §8 R-C3-1; C1 §2 rows 1 and 4; plan §3 P7 | Say `evaluate({live:true})`, gate = `rollup.status === 'no-go'` only; test both cases. |
| D-23 | MINOR | P5/P6 | `reset()` clears latches; P6's recompute "preserving operator-door latches" cannot after a system reset wiped them. M2 says the feed re-runs from the boot-frozen pack+profile; operator latches are session-scoped by nature — but the plan should say it. | plan §3 P5, P6; spec §5 M2 | Record: operator-door latches do not survive `system:reset` (the session ended); profile-door latches are re-applied. |
| D-24 | MINOR | DoD (e), (h) | (e): with shipped defaults (3 s ×2) respawns land at 3, 9, 21, 45 s, so five kills inside 60 s succeed only if the killer strikes within ~15 s of the fifth respawn; the window semantics (sliding vs first-exit) are unstated. (h): the scanner fetches the same server's pack on load, so "loaded with a different pack hash" needs a restart of the orchestrator with a different `packPath` on the same port while the page stays open; `restartOrchestrator` reuses `serverPackPath`. | plan §3 P10/P11, §5 (e),(h); `backend/tests/e2e/setup/test-server.js:397-425` | (e) pins its strategy through `HOST_CONFIG_PATH` and defines the window as sliding over exit timestamps; (h) names the restart-with-`packPath` mechanism. |
| D-25 | MINOR | P2's citation | P2 cites M7 ("the service verdict gates its resource verdicts"); M7's rule is unqueryable inventory ⇒ UNKNOWN, not dormant. P2 is in fact C1 §2 row 3 ("dependent cues/surfaces disabled") applied to needs and is consistent with CS.1 adjudication 4's reasoning (both dormant doors require a CHOSEN absence; `installed:false` is one). The profile-contradiction case (bindings present for a role under `lighting.instruments.installed:false`) is undecided. | spec §5 M7; §9 adjudication 4; plan §3 P2 | Re-cite P2 to C1 §2 row 3 + adjudication 4; rule the contradiction (endpoint wins; bindings under a dormant family are ignored with a warn). |
| D-26 | MINOR | Stage assignment | Ratified CS.2 = "sticky dormant with BOTH doors"; the plan builds the operator door in T3 (CS.3). A re-slice, unrecorded. | spec §8 CS.2 text; plan §4 T3 | One line in §1 recording the re-slice and why (the door rides the verb commands). |
| D-27 | MINOR | T7 | T7 bundles the 27 census-sourced §2.2 sweep rows plus hygiene, workflows, records, dist, every gate, and the adversarial review into one sonnet task; §7 does not enumerate the rows, so the required-before-merge point carries hidden bulk. | census §2.2 (27 rows, `grep -c` on the id table); plan §4 T7, §7 T7 | Split T7a (the §2.2 rows, enumerated) from T7b (close); §7 lists T7a's rows. |
| D-31 | MINOR | T4 resource arms vs D-C2.1 | D-C2.1 (r1, standing per §6) makes `validateCommand` "the per-command resource arm of the same sweep — its first production caller", and C1 §3 group 5 says "via validateCommand sweep". The plan's §2 records the zero-caller fact, but T4 builds its pack-refs/bindings/media arms in `preflightService` without naming `validateCommand` as the caller — a second resource-existence opinion beside the one that gates commands at fire time. | `backend/src/services/commandExecutor.js:912-958` (`validateCommand`); census §5 (zero production callers); spec §3 D-C2.1; C1 §3 item 5 | T4: the resource rows are produced by iterating the pack's cue commands through `validateCommand(action, payload)` (one truth); `preflightService` adds only what it cannot express (token videos via the `video-file` need). |
| D-28 | NOTE | Vocabulary | Terms the plan uses that CONTEXT.md does not define and that the build will need: flap window, gave-up, door (as a field), feed, require gate, preflight stamp, limits, "Put back in service", `disabledBy`, `dormancyDisabledCues`, "Block 2a". CONTEXT §4 "Held item" ("a held video has no expiry") becomes false after P13; "Supervisor" lacks flap/gave-up; "Status with verbs" lacks the inverse verb. No use of "planning view", "expected", or "degraded" as live states was found (grep). The summary text "N/M operational" keeps the renderer's existing word; CONTEXT's word is healthy. | `grep -n` on the plan; CONTEXT §4 lines 324-330, 355-360 | `/domain-modeling` rows for: flap window, gave-up, door, feed, require gate, preflight stamp, limits, service domain (verdict sheet), Put back in service; fix the Held item expiry sentence at T3. |
| D-29 | NOTE | Header | The plan's census pointer is `e87f8c5`; HEAD is `145c05d`; the plan changed under review (R5, R6, §8). | `git log e87f8c5..HEAD` | Restamp at ratification. |
| D-30 | NOTE | P11 | CS.1's record names other host facts (engine user, HA container name, `CHROMIUM_BIN`); `host.json` carries only `supervision`. | spec §9 (VLC-as-root, container `homeassistant`, `CHROMIUM_BIN`) | Record them as `host.json` headroom or include `processes.<id>.bin/user`. |

## 2. Ruling-by-ruling walk (c2c3 §8 + §5) → plan

| Ruling | Implemented by (task / test) | Status |
|---|---|---|
| R-C2-1 self-heal + backstop | T5: `packLoader.refresh()`, heal test, backstop test; DoD (h) | Implemented, EXCEPT the channel (D-13) |
| R-C2-2 host arm, paper/live + profile id | T4 host arm; P8 `depth` + `profileId` | Implemented; limits statement missing (D-11) |
| R-C3-1 unbound idle-loop = DORMANT; require gate + typed override | `resolution.js:113-122` (CS.1); P7/T1a startGame tests; DoD (f) | Implemented; depth unstated (D-22) |
| R-C3-2 enum `healthy|down|dormant`, `degraded` deleted, three sites + validator | T1a contracts + registry + cross-check enum assertion | Implemented; two consumers missing (D-19), service-case fault (D-2) |
| R-C3-3 supervisor (bounded, backoff, host config, escalate with verbs; out-of-service door) | T3 P10/P11/P12; DoD (e) | Implemented for the eight; Chromium has no escalation (D-1); verbs split (D-7) |
| r3 plural profiles / absences dormant | P1/T1b | Implemented; rung-1/Tier L profile consequence unhandled (D-3) |
| r3 paper/live labeling | P8 | Implemented |
| r3 witness register, rung-1 harness, capability manifests | CS.1 (done); T1b's leg "capability-gated" | Inherited |
| CS.2 (dormancy lifecycle + enum, both doors, require gate, feed points) | T1a (+ operator door in T3) | Implemented; re-slice unrecorded (D-26) |
| CS.3 (supervisor + verbs + self-heal + hold policy) | T3, T5 | Implemented; stop-point/parity split (D-7) |
| CS.4 (all six C1 §3 groups + host, panel + CLI, cert warn) | T4, T6 | Group 6 sinks + group 5 consumer-hash rows missing (D-10) |
| CS.5 (dual-pack Tier L + rung-1 green, ratchet, review, records) | T7 | Present; gates fail under D-3 as drafted |
| M1 sticky dormant, feed after init | P5, P6 | Implemented |
| M2 reset re-runs the feed | P6, T1a systemReset test | Implemented (operator latch: D-23) |
| M3 fireCue returns a refusal; ack carries it | P3/P4, T1a executor tests | Implemented |
| M4 two sets; enable-on-dormant refused | P4 | Implemented |
| M5 auto-discard = video_busy only; fault holds expire at session end; dormant never holds | P13/T3 `expireHolds` | Implemented (T3); compound-cue hang under P3 (D-4) |
| M6+S1 endpoint interior = C1 §1 verbatim; pack-side onAbsent in manifest; device-class | P1/T1b; CS.1 `packNeeds` | Implemented |
| M7 service gates resources; unknown never faults | T4 bindings/network arms `unknown` | Implemented; P2 mis-cites it (D-25) |
| M8 every `'healthy'/'down'` consumer swept; stamp `undefined→null` | T1a list; T6 null guard | Two consumers missed (D-19); resolver service case (D-2) |
| S2 role-unbound is command-level; cue-level disables = dormant-service only | CS.1 resolver + executor (done); P3 | P3 deviates for mixed cues, unrecorded (D-5) |
| S3 network + devices arms IN | T4 network, devices | Devices arm incomplete (D-10) |
| S4 partial absorption of the checklist | T4 checklist restructure | Implemented; limits (D-11) |
| S5 capabilities.js dormant state; E2E_PROFILE_PATH plumbing | T1a; plumbing exists (`test-server.js:150`) | `requireDegraded` missed (D-14) |
| S6 named DoD flow (a)–(d) | §5 (a)–(d) | Present; mixed cue absent (D-20) |
| S7 player-scan wire refuses on dormant vlc; video holds expire at session end | P13 (`scanRoutes` 409; `videoQueueService.expireHolds`) | Implemented (wording D-18) |
| S8 census corrections | plan §2 | Carried |
| S9 §8.5 tests are debts, not gate criteria | T5 "the two owed pack-loader tests"; §7 T5 | Implemented as debts |
| S10 estimate joins the batch | superseded by R8 (no pricing) | Recorded |

## 3. ROADMAP §4 "Block 2" deliverables → plan

| Deliverable | Where | Status |
|---|---|---|
| Third health word at every consumer | T1a | See D-2, D-19 |
| Supervisor: bounded, loop detection, escalate with a verb | T3 P10 | Chromium gap (D-1) |
| Sticky dormancy, two doors, session-start gate + typed logged override | T1a P5–P7, T3 P12 | Implemented |
| GM scanner heals its own stale pack | T5 P14 | Channel (D-13) |
| Preflight in the panel + CLI + short human checklist; **honesty rule (Q13)** | T4, T6 | Paper/live yes; "states its own limits on its face" missing (D-11) |
| A plain host-config file for restart strategies | P11 `backend/config/host.json` + schema + `HOST_CONFIG_PATH` | Implemented |
| Block close: dual-pack E2E + rig CI green, review, records | T7 | Present |
| §5 "scoreboard liveness as a first-class check" | T4 row only | On-demand, no live escalation (D-1) |

## 4. The fifteen pins — refinement or contradiction

| Pin | Verdict |
|---|---|
| P1 | Refinement (C1 §1 interior verbatim + a service map the spec leaves open). Semantics (profile-alone vs manifest-declared) must be pinned — D-3. |
| P2 | Refinement consistent with adjudication 4's reasoning (a chosen absence) — allowed; mis-cited to M7 — D-25. Unbound-under-installed stays FAULT: correct. |
| P3 | Contradicts the LETTER of D-C3.2/S2 for mixed cues, argued on an unratified phrase; compound-cue hang — D-4, D-5. Must be recorded as a deliberate refinement or reverted to whole-cue disables. |
| P4 | Matches M4. |
| P5 | Matches M1; operator latch on reset — D-23. |
| P6 | Matches M1/M2 feed points. |
| P7 | Matches R-C3-1; depth — D-22. |
| P8 | Second verdict vocabulary — D-12; limits — D-11. |
| P9 | Not a discrete event: allowed; under-recorded — D-16. |
| P10 | Contradicts R-C3-3 for Chromium — D-1; cross-ref error ("P12" should read P8/T4). |
| P11 | Matches R-C3-3 + R6. |
| P12 | Matches R-C3-3's verb set (Re-route/Release-Discard reuse existing commands — acceptable if the ROW carries them, T5 lists only three buttons). |
| P13 | Matches M5/S7. |
| P14 | Contradicts R-C2-1's "connected orchestrator" for Pages-served stations — D-13. |
| P15 | Matches R5 (now ruled). |

## 5. Parity surface — consumers the plan misses (census §1 as checklist)

Covered by T1a: `serviceHealthRegistry.js:42,72`; `resolution.js:144` (named, but the dormant branch is not — D-2); `commandExecutor.js:178-187` wording; `asyncapi.yaml:664,2597`; `openapi.yaml:2005`; `capabilities.js:61`; `HealthRenderer.js:48,83,136`; scanner cross-check test.
Missed: `backend/tests/e2e/helpers/assertions.js:187`; `backend/tests/rung1/audit-flows.js:89-95` (in T1a); `capabilities.js:87-95` `requireDegraded`; `commandExecutor.js:832` (`service:check` text); `videoQueueService.js:866-871` (`canAcceptVideo` message); `videoQueueService.js:31` (recoverable emit keys on `'healthy'` — correct after P5, note only). Outside the census grep scope but enum-pinning: `backend/tests/unit/services/serviceHealthRegistry.test.js:106` (asserts `'degraded'` is rejected — must become "`'dormant'` accepted via `markDormant` only, or via `report`?": P5 must say whether `report(id,'dormant')` is legal), `ALNScanner/tests/unit/ui/renderers/HealthRenderer.test.js`, `ALNScanner/tests/unit/core/stateStore.test.js` (fixtures only).

## 6. Scope inventory and stop points (§7)

Complete against §8 and fix vehicle §2.1 except F-P2-3 (D-9) and the two S3/C1 §3 rows (D-10). Stop points: T1b coherent; T1a coherent only after D-2/D-3/D-6 (else the candidate tree is red-for-dormant in the preflight, hollows the Tier L gate, and advertises four unimplemented commands); T3 not coherent as drafted (D-7); T4 coherent; T5 as drafted depends on T3 (D-7); T6/T7 coherent. Nothing is "priced at zero" in time terms (R8 forbids pricing) but two items are priced at zero in SCOPE: F-P2-3 and the §2.2 rows (D-27).

## 7. DoD pins — executability

| Pin | Where it runs | This container (baseline) |
|---|---|---|
| (a)(b)(c)(f) | backend integration tests + Tier L dormant leg (`E2E_PROFILE_PATH` exists: `test-server.js:150`, `session-env.js:129`) | Integration: yes. Tier L: no (no Chromium/Playwright browser, no pipewire/vlc). CI: yes. |
| (d) | scanner unit (jsdom) + Tier L | Unit yes; Tier L no (dist rebuild is possible here). |
| (e) | rung-1 only (`rung1.yml`) | No (docker daemon, vlc, pipewire, dbusmock all absent per baseline §4); CI rung1: yes; timing fragile (D-24). |
| (g) | unit/contract | Unexecutable as worded (D-8). |
| (h) | Tier L toy leg with an orchestrator restart | CI only; mechanism unstated (D-24). |

## 8. Checked and found sound

- The three contract enum sites and the registry validator are exactly where the plan says (`asyncapi.yaml:664`, `:2597` with `degraded`, `openapi.yaml:2005`, `serviceHealthRegistry.js:42`).
- No new discrete WS event; `MESSAGE_TYPES` untouched; the domain route is `service:state` — honored (P9).
- P5's sticky semantics match M1 verbatim; P6's feed points match M1/M2 (post-init before revalidation, create, restore, reset).
- P4/M3/M4: two sets, `fireCue` returns a reason, `cue:enable` refusal, ack carries the refusal — matches; the executor's unconditional "Cue fired" ack at `commandExecutor.js:622-633` and `fireCue`'s `return;` at `cueEngineService.js:505-508` are correctly identified as the M3 bug.
- P13 matches M5/S7; `scanRoutes.js:131` already carries the `409 {status:'rejected'}` wire; `setAutoDiscard`'s single caller is the `video_busy` branch (`cueEngineService.js:632`).
- P1's interior is C1 §1 verbatim (`display.main {installed, output?}`, `audio.sinks[{id, installed, btAddress?, label?}]`, `lighting.instruments {installed, provider}`, `stations {count}`, `personal {expected}`); `installed:false` = absent is consistent with the schema's own description (`installation-profile.schema.json:70`).
- P7 matches R-C3-1 (typed = GM types the reason; logged; stamped); `startGame()`'s status guard exists at `sessionService.js:263`.
- P10/P11 match R-C3-3 + R6 for the eight; `ProcessMonitor`'s output-resets-counter bug is real (`processMonitor.js:136-138`); VLC's flat 3 s retry is real (`vlcMprisService.js:120`); the four supervision shapes are as the census says.
- P12's `service:*` floor is already `show-control` by prefix (`grants.js:80`); `session:` is `session-lifecycle`.
- P14's toast/backstop shape matches R-C2-1; the scanner already sends `packHash` in the handshake (`orchestratorClient.js:101`) and the server warns (`socketServer.js:139-144`); `sync:full.pack` exists (`syncHelpers.js:150`); the two §8.5 tests are absent (`packLoader.test.js:199` is a structural signal check, not the behavioral timeout).
- P15 matches the R5 ruling; `socketServer.js:53` scopes the check to `gm` as the census says.
- `E2E_PROFILE_PATH` is honored by the E2E harness (baseline §5), so T1b's third leg is buildable.
- The `video-file` need kind (CS.1 record gap) is in T4; F-P2-7 (pack integrity) in T4; F-P2-8 (profile identity) in T1a; LC-6/P9b-9 and P9b-4/8 in T7.
- Vocabulary: "not installed tonight", "taken out of service" (as "out of service"), door, latch, self-heal, feed, stamp, paper/live, rung, green machine all match CONTEXT/spec; no "planning view"/"expected"/"degraded" used as live states.
- `validateCommand`'s zero-caller state is correctly recorded in plan §2 (census §5 confirms); its role as the preflight's resource arm is D-31.

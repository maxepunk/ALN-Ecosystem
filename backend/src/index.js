/**
 * Main Entry Point for ALN Orchestrator
 * 
 * This file serves as the primary entry point for the application.
 * It simply requires the server module which handles all initialization.
 */

require('./server');